# v1.5.6 Edge-Case-Sweep — Rohbericht des Multi-Agent-Laufs

**Lauf:** 2026-08-15, 15 Agents, 7 Flaechen, 0 Fehler, ~2,58 Mio Subagent-Tokens, 924 Tool-Calls, 38 Min.
**Verfahren:** pro Flaeche ein Finder mit Beweispflicht (datei:zeile auf BEIDEN Seiten + Fehlerszenario), danach ein adversarieller Durchgang, der auf "abgelehnt" steht, solange er die Zeilen nicht selbst geoeffnet hat.

> ⚠ **Status:** Das hier ist der ROHBERICHT der Agents, nicht mein geprueftes Ergebnis.
> Ich habe stichprobenartig nachverifiziert und dabei bereits eine falsche Ueberschrift gefunden (Fund 10).
> Vor jeder Umsetzung gilt: Zeile selbst oeffnen. Siehe Abschnitt "Mein Pruefstand" unten.

## Mein Pruefstand (selbst am Code nachgeschlagen)

| Fund | Mein Befund |
| --- | --- |
| 1 — Master-Schalter | **BESTAETIGT.** elvui_tags.lua prueft nur db.elvuiTag (:39, :108), grid2_status.lua nur db.grid2Status (:32, :41). db.enabled kommt in beiden Dateien nicht vor. Gegenprobe: blizzdm.lua 4x, danders 1x, core.lua 13x. Die Asymmetrie ist real. |
| 10 — blizzDM | **UEBERSCHRIFT FALSCH.** Es existiert kein VALIDATORS.blizzDM (grep leer), und ein "Reset to Defaults" finde ich im UI nicht. RICHTIG ist: ValidateDb iteriert pairs(defaults) (core.lua:1653) und blizzDM steht nicht in den Defaults, weil es ein Tristate ist (nil=auto, init.lua:37 nur als Kommentar). Ein korrupter Wert entgeht der Validierung also — aus anderem Grund als behauptet. |
| 2-9, 11-18 | **NOCH NICHT von mir geprueft.** Agent-Behauptung + adversarielle Gegenpruefung, mehr nicht. |

## Die 18 Funde

### 1. Master switch db.enabled is ignored by both ElvUI and Grid2 - /dilvl off leaves the tags on screen and freezes them forever

- **Flaeche:** elvui-grid2 · **Schwere:** high · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/elvui_tags.lua:39 (buildIlvl: `if not db or not db.elvuiTag then return "" end`) and grid2_status.lua:32 + :41 (IsActive/GetText: `if not db or not db.grid2Status`)
- **Gegenstelle:** n/a (internal contract, proven three ways): locales/enUS.lua:35 = "Master switch for the entire addon. Disable to silence every output channel without uninstalling."; ui/preview_widgets.lua:219-231 blanks the mock unit frame when db.enabled==false, under the caption locales/enUS.lua:95 "Sample frame - Grid2 / ElvUI render analogously"; every other channel DOES gate on it - blizzdm.lua:340, blizzdm.lua:929, core.lua:765, core.lua:806, core.lua:965, core.lua:1010
- **Fehlerszenario:** User has elvuiTag=true (or grid2Status=true) and a party member cached at 284. He runs `/dilvl off` (core.lua:1827-1831) or unticks "Enable Details! iLvl Display" (ui/page_general.lua:87-91). core.lua:1719-1726 clears the Details! bars and calls NotifyElvUI(); our elvui callback runs RefreshMethods, buildIlvl re-evaluates, sees db.elvuiTag still true and re-renders "[284]". Grid2's IsActive still returns true and GetText still returns "284 4P". Second half: with db.enabled=false, core.lua:1433 (PLAYER_REGEN_ENABLED), core.lua:1454 (ENCOUNTER_END) and core.lua:1472 (GROUP_ROSTER_UPDATE) all return early, so QueueGroupInspect never runs again. The number on the ElvUI/Grid2 frame is now frozen at its last value for the rest of the session, looks completely normal, and the settings preview actively tells the user it is gone.

> **Korrektur aus dem Widerlegungs-Durchgang:** The second half - "QueueGroupInspect never runs again... frozen at its last value for the rest of the session" - is FALSE and should be struck. Three cache-writing paths carry no db.enabled gate at all, and API.GetCacheData (core.lua:2659-2662) reads ilvlCache directly, so the tag keeps moving: (a) INSPECT_READY, core.lua:1359-1383 - handles ANY inspect, including ones Details!'s own queue triggers, and writes ilvlCache[guid] at :1383 then calls NotifyElvUI at :1401; (b) UNIT_INVENTORY_CHANGED, core.lua:1487-1512 - ungated, and :1512 `C_Timer.After(2, QueueGroupInspect)` re-runs the sweep on any cached member's gear swap; (c) the LibOpenRaid GearUpdate callback, core.lua:1252-1271, ungated, writes the cache at :1266 and NotifyElvUI at :1271. Plus `/dilvl inspect` at core.lua:1893-1894 is ungated, and the PLAYER_ENTERING_WORLD bootstrap at core.lua:1320-1324 schedules three sweeps with no db.enabled test. Accurate statement: with db.enabled=false the three EVENT-DRIVEN refresh triggers (core.lua:1433, :1454, :1472) and the ticker (core.lua:1010) stop, so updates become erratic and lag rather than frozen - which is arguably worse for diagnosis, because the number still moves and therefore looks alive while the master switch is off.

### 2. ElvUI tag returns "" instead of nil, so oUF literal prefixes/suffixes are never suppressed

- **Flaeche:** elvui-grid2 · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/elvui_tags.lua:39, :42, :45 - all three no-data paths `return ""`
- **Gegenstelle:** e:/dev/gaming/wow-addons/ElvUI/ElvUI_Libraries/Game/Shared/oUF/elements/tags.lua:16-18 (contract: "Literals can be pre or appended ... The literals will be only displayed when the function returns a non-nil value") and tags.lua:707 `return str and format('%s%s%s', prefix or '', str, suffix or '') or nil` - the nil test is the ONLY suppressor, and "" is truthy in Lua; also tags.lua:755 `tagBuffer[i] = type(fnc) == 'function' and fnc(...) or ''`
- **Fehlerszenario:** User puts `[name] [dilvl< ilvl]` in ElvUI -> Unit Frames -> Party -> Name text (oUF suffix syntax, tags.lua:1058-1060 StripTag). Before anyone has been inspected, or with `/dilvl elvui off`, our func returns "" -> CreateTagFunc at tags.lua:702-708 takes the truthy branch and emits `"" .. " ilvl"` -> the frame permanently reads "Raza ilvl" with no number, and no error. Same root cause in the plain documented form: `[name] [dilvl]` builds frmt "%s %s" (tags.lua:726) and SetFormattedText("Raza", "") at tags.lua:759 leaves a permanent trailing space, which visibly off-centers a CENTER-justified name text. Returning nil in all three paths fixes both.

> **Korrektur aus dem Widerlegungs-Durchgang:** Drop the second scenario and the phrase "Returning nil in all three paths fixes both". Returning nil does NOT remove the trailing space in `[name] [dilvl]`: tags.lua:755 is `tagBuffer[i] = type(fnc) == 'function' and fnc(...) or ''`, which coerces a nil return to '' before SetFormattedText at :759, so "Raza " is produced identically whether we return "" or nil. The space is a tag-string literal between two brackets, and tags.lua:21-23 documents that such literals "will be displayed in the output string regardless of whether the surrounding tag functions return a value" - it is not ours to suppress. Accurate finding: only the oUF prefix/suffix form `[dilvl>pre]` / `[dilvl<suf]` is affected, and returning nil at elvui_tags.lua:39, :42, :45 fixes exactly that.

### 3. Grid2 never applies our tier colour - db.colorIlvl is silently a no-op on Grid2 frames

- **Flaeche:** elvui-grid2 · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/grid2_status.lua:28 (`DiLvl.GetColor = Grid2.statusLibrary.GetColor`), :39-50 (GetText returns bare `tostring(cached.ilvl)` with no |c escape) and :65-68 (default dbx hardcodes color1 = white). Compare elvui_tags.lua:51-55, which DOES wrap the number in API.GetIlvlColor().
- **Gegenstelle:** E:/World of Warcraft/_retail_/Interface/AddOns/Grid2/GridUtils.lua:220-223 - `GetColor = function(self) local c = self.dbx.color1; return c.r, c.g, c.b, c.a end` (a constant, it cannot depend on the unit); E:/.../Grid2/modules/IndicatorText.lua:320-329 TextColor_OnUpdate, whose else-branch is `frame.Text:SetTextColor(1,1,1,1)` and which only runs at all if the user separately maps the status onto the `<indicator>-color` sidekick registered at IndicatorText.lua:342-348
- **Fehlerszenario:** User has "Color by gear tier" ON, enables the Grid2 channel and assigns status `dilvl` to a Grid2 corner-text indicator. Details! bars show orange "[284]" (core.lua:440), the ElvUI tag shows orange "[284]" (elvui_tags.lua:52), Grid2 shows flat white "284". Toggling `/dilvl color` fires ApplySettingChange (core.lua:1727-1730) -> NotifyElvUI -> our Grid2 callback -> UpdateAllUnits -> byte-identical text. Nothing errors, and the addon's own preview (ui/preview_widgets.lua:250-252 paints the mock number with ilvlColor, caption locales/enUS.lua:95 "Grid2 / ElvUI render analogously") promises the opposite.

### 4. Grid2 text indicator with "Show stack" enabled renders 0 instead of the item level - we inherit GetCount() = 0, which is truthy

- **Flaeche:** elvui-grid2 · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/grid2_status.lua:25-56 - DiLvl overrides IsActive, GetText, OnEnable, OnDisable, UpdateDB and GetColor, but never GetCount
- **Gegenstelle:** E:/World of Warcraft/_retail_/Interface/AddOns/Grid2/GridStatus.lua:29-31 `function status:GetCount() return 0 end` (statusPrototype default is the number 0, NOT nil) consumed by E:/.../Grid2/modules/IndicatorText.lua:188-201 `Text_OnUpdateS`: `local count = status:GetCount(unit); if count then Frame.Text:SetText(count) ... else Text_SetText(...)`; that handler is selected at IndicatorText.lua:311-312 when dbx.stack is set and duration/elapsed are not; the toggle is a normal GUI checkbox at E:/.../Grid2Options/modules/indicators/IndicatorText.lua:124-135 ("Show stack")
- **Fehlerszenario:** User assigns `dilvl` to a Grid2 text indicator and, for an unrelated aura status on the same indicator, ticks Indicators -> <text indicator> -> Show stack (and leaves Show duration/Show elapsed off). Text_UpdateDB switches OnUpdate to Text_OnUpdateS; GetCount returns 0; `if count then` is true because 0 is truthy in Lua; SetText(0). Every party/raid frame now reads "0" where the item level was, GetText is never called, and no error is raised. Fix: `function DiLvl:GetCount() end` (return nil) so the handler falls through to Text_SetText.

> **Korrektur aus dem Widerlegungs-Durchgang:** Only a scoping nit: the trigger does not require an "unrelated aura status on the same indicator". Because dbx.stack lives on the indicator (IndicatorText.lua:303/311), any user who has ever ticked "Show stack" on the text indicator they later assign dilvl to gets "0", with no second status involved. Note also that this same missing-override class covers GetCountMax (GridStatus.lua:33-35, returns 1) for bar indicators.

### 5. Both channel callbacks pcall themselves, so the per-callback error budget and the /dilvl debug callback-error line are permanently blind for exactly the two channels they exist for

- **Flaeche:** elvui-grid2 · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/elvui_tags.lua:118-121 (`pcall(function() E.oUF.Tags:RefreshMethods("dilvl") ... end)`) and grid2_status.lua:71 (`if DiLvl.UpdateAllUnits then pcall(DiLvl.UpdateAllUnits, DiLvl) end`)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:2795 `local ok, err = pcall(cb, playerName)` - our inner pcall always makes ok true, so core.lua:2798-2801 resets _callbackErrors[name] to 0 on every single dispatch; core.lua:110 documents "ElvUI / Grid2 -> _callbackErrors[name] below -> per-callback unregister"; core.lua:2086 dumps those counters in the bug report. The refresh entry point itself is a plain table field: ElvUI_Libraries/Game/Shared/oUF/elements/tags.lua:1070 `RefreshMethods = function(_, tag)`.
- **Fehlerszenario:** ElvUI reworks oUF's tag cache (RefreshMethods renamed, or its bracketFuncs/tagStringFuncs walk at tags.lua:1076-1096 restructured so the call throws). Our pcall swallows the error on every INSPECT_READY, gear swap and GROUP_ROSTER_UPDATE. The tag stops updating and freezes at whatever it last rendered. `/dilvl debug` prints zero callback errors (core.lua:2086) and "ElvUI-tag: ON" (core.lua:2006), so the pasted bug report contains no signal at all - this is structurally the same trap as the instance.baseFrame / Ambiguate regressions. Minimum fix: record `local ok, err = pcall(...)` and surface a counter (or drop the inner pcall and let core.lua:2792-2828 do its job).

> **Korrektur aus dem Widerlegungs-Durchgang:** Two things the finding does not say, both of which change the recommended fix. (1) The inner pcall is deliberate and documented, not an oversight - elvui_tags.lua:94-98 explains it was moved inside the closure precisely because dereferencing E.oUF outside the protection threw for real during ElvUI profile switches and five of those unregistered us. So "drop the inner pcall" would reintroduce a known regression; only the "capture ok/err and surface a counter" half is safe. (2) The trigger is hypothetical - I found no current input that makes RefreshMethods throw: E.oUF is set at ElvUI/Game/Shared/General/Initialize.lua:45, and RefreshMethods' magic-char escape at tags.lua:1074 does not touch the colon in "dilvl:plain". Report this as instrumentation blindness with a proven wrong output (a diagnostics dump asserting health it cannot measure), not as a live breakage.

### 6. "host: absent" for Grid2 is NOT a code defect - Grid2 is disabled per character; but the code that reports it misdiagnoses the cause

- **Flaeche:** elvui-grid2 · **Schwere:** info · **Still (keine Fehlermeldung):** False
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:2013-2015 (`Grid2 and "loaded" or "absent"`), core.lua:2467-2468 (`if not Grid2 then print("... Grid2 not installed.")`), ui/page_diagnostics.lua:59, ui/page_general.lua:66 - all four infer presence purely from `Grid2 ~= nil`
- **Gegenstelle:** E:/World of Warcraft/_retail_/WTF/Account/GAZAROTH/Blackhand/Baboroth/AddOns.txt (newest of all 353 AddOns.txt, written 2026-08-13 22:45) contains the line `Grid2: disabled`; same in Blackhand/Quinroth/AddOns.txt (2026-08-12 20:49). Corroborated by E:/.../WTF/Account/GAZAROTH/SavedVariables/Grid2.lua, last written 2026-04-27 - Grid2 has not loaded since April. The addon itself is present and current: Grid2/Grid2.toc:4 Version 4.0.8, :1 Interface 120100, :14 LoadOnDemand: 0, and Grid2 sets the global at GridCore.lua:5.
- **Fehlerszenario:** A disabled addon is never loaded, so the `Grid2` global is nil and grid2_status.lua:20 silently returns before registering anything - correct behaviour. Two real defects sit next to it: (1) core.lua:2468 tells the user "Grid2 not installed" when it IS installed, merely unticked, which sends the user looking for a download instead of the addon list; (2) `/dilvl grid2 on` refuses to set the flag while Grid2 is absent (core.lua:2467-2474) but the Settings-UI checkbox at ui/page_channels.lua:208-210 sets grid2Status with no host check at all, so the two entry points disagree on whether the state is even reachable. Practical consequence for this review: the Grid2 channel has never executed on this installation, which is why findings 3 and 4 have gone unnoticed.

> **Korrektur aus dem Widerlegungs-Durchgang:** The slash-vs-UI divergence is worse than described and the sharper half is missed. core.lua:2471 `/dilvl grid2 on` calls RestoreCallback("grid2") and core.lua:2458 `/dilvl elvui on` calls RestoreCallback("elvui"), but the UI path routes through ApplySettingChange, whose branches for those keys (core.lua:1771-1774) call ONLY NotifyElvUI - no RestoreCallback. So once a callback is parked by the 5-error limit at core.lua:2820-2821, the Settings-UI checkbox can never revive it and the user has no way to know; only the slash command can, and for Grid2 the slash command is exactly the one that refuses while the host is nil. Note the ApplySettingChange dandersText branch (:1779-1781) does call its reset helper, so elvui/grid2 are the only two channels with this hole. Also: the fix for the wrong message is available in-tree - ui/page_diagnostics.lua:61-62 already uses C_AddOns.IsAddOnLoaded, so C_AddOns is reachable and the four sites can distinguish not-installed from installed-but-unticked instead of inferring from `Grid2 ~= nil`.

### 7. seenGuids is marked before the name is known, so a secret UnitName disables the cache fallback that exists for exactly that case

- **Flaeche:** data-edge · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:349 (`seenGuids[guid] = true`) executes BEFORE core.lua:350-351 (`local name, realm = SafeUnitName(unit); if name then`), and core.lua:366 (`if not seenGuids[guid] and cached.ilvl and cached.name then`) then skips that GUID
- **Gegenstelle:** e:/dev/gaming/wow-addons/wow-ui-source/Interface/AddOns/Blizzard_APIDocumentationGenerated/UnitDocumentation.lua:2382-2385 (`Name = "UnitName"`, `SecretWhenUnitNameIdentityRestricted = true`), which is precisely why SafeUnitName returns nil,nil at secrets.lua:98-101
- **Unsere unausgesprochene Annahme:** That "we have a live unit token for this GUID" implies "we can produce a usable name for it", so the ex-group fallback loop is redundant for roster members. In a restricted instance UnitName returns a secret, SafeUnitName converts that to nil (secrets.lua:98-101, counted as stats.unitNameBlocked), and the live branch stores nothing — but the GUID has already been claimed as "seen".
- **Fehlerszenario:** Input: a restricted/event instance (the Slave Pens case the addon already documents at core.lua:1116-1123) where UnitName("raid7") is secret. ilvlCache[guid] holds a perfectly good plain entry {ilvl=284, name="Torvi-Onyxia"} from an earlier inspect. RebuildNameIlvlMap: :348 sees cached.ilvl -> :349 sets seenGuids[guid]=true -> :350 SafeUnitName returns nil -> nothing is stored. The fallback loop at :365 then skips the GUID because seenGuids[guid] is true. Output: nameToIlvl has no key for Torvi, the Details! bar shows no iLvl tag at all, and /dilvl debug reports the entry as present in the cache — so the cache looks healthy while the display is empty. Moving line 349 to inside the `if name then` block restores the fallback. (I verified the code path and the API precondition; I could not verify in-game that raid tokens specifically hit the restricted-identity predicate.)

> **Korrektur aus dem Widerlegungs-Durchgang:** The finder's stated OUTPUT does not follow, and should not be used to justify the fix. In content where UnitName is secret, Details' own bar text on the live path is a secret too — class_damage.lua:3365 branches on `issecretvalue(actorName)` and hands a secret to lineText1 — and our SetText hook detects that first at core.lua:810-822, nulls barCleanText and returns before any name lookup happens. So "the Details! bar shows no iLvl tag" would be the outcome regardless of the seenGuids ordering; the map gap is not what the user would be seeing there.

What I could NOT check, and what the finding therefore rests on: (a) that any live 12.1 content actually trips SecretWhenUnitNameIdentityRestricted on raidN/partyN tokens while leaving UnitGUID declassified — I only established that the two predicates are distinct in the API docs, not that they diverge in practice; (b) that the affected consumer is a name-keyed host at all — blizzdm.lua resolves through API.ResolveGUIDByName (blizzdm.lua:410, :421, :433, :453, :462) and then reads ilvlCache by GUID, so it may not depend on nameToIlvl for this case either.

Accurate framing: this is a proven ordering defect in RebuildNameIlvlMap that makes the ex-group fallback unreachable for exactly the GUIDs it was written to cover, with an unverified trigger and no demonstrated user-visible symptom. Fix it because it is free, not because the described breakage was observed.

### 8. Settings window gated on MayBeInCombat() (secret⇒true) — /dilvl in combat CLOSES an open window and promises a re-open that never happens

- **Flaeche:** ui-settings · **Schwere:** high · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/slash_ui.lua:29-32 and ui/main_frame.lua:380-383, 408-421, 428-436
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/secrets.lua:147-151 (MayBeInCombat: secret⇒TRUE) vs secrets.lua:141-145 (IsInCombatSafe: secret⇒FALSE); e:/dev/gaming/wow-addons/wow-ui-source/Interface/AddOns/Blizzard_APIDocumentationGenerated/RestrictedActionsDocumentation.lua:44-52 (InCombatLockdown returns a plain bool, no Secret* fields)
- **Unsere unausgesprochene Annahme:** That MayBeInCombat() is the right polarity for gating the settings window, and that Toggle+defer compose safely. Two things are wrong. (a) BuildFrame touches no protected UI at all — it is CreateFrame("Frame", ...) on UIParent (main_frame.lua:201) — so no combat gate is needed; if one is kept, IsInCombatSafe (secret⇒false) is the wrapper whose semantics match the comment. Every other non-protected call site in the addon uses IsInCombatSafe (core.lua:603,1056,1101,1472,1500; blizzdm.lua:311,936,1079,1173,1189,1239,1260); MayBeInCombat is reserved for writes into Details!' own FontStrings (core.lua:723,842,912). The UI is the one exception. (b) slash_ui.lua prints the deferral message and then unconditionally calls main.Toggle, which for a visible window calls main.Close() — and Close() never sets ns._uiOpenPending, so the PLAYER_REGEN_ENABLED handler at main_frame.lua:430-436 has nothing to act on.
- **Fehlerszenario:** Settings window is open. Player pulls a boss. Player types /dilvl to dismiss it. Chat prints "Settings UI deferred until combat ends" (slash_ui.lua:30) and the window closes (Toggle→Close, main_frame.lua:416-417). Combat ends; PLAYER_REGEN_ENABLED fires; ns._uiOpenPending is nil so nothing reopens. The message was false. Typing /dilvl again during the next pull repeats it. Amplification: the code comments at main_frame.lua:376-379 and slash_ui.lua:25-28 state that InCombatLockdown() returns a secret-wrapped false inside restricted instances. If that is true, MayBeInCombat() returns true for the ENTIRE instance including out of combat, so /dilvl can never open the settings window there at all — the exact outcome the comment claims the choice of MayBeInCombat avoids ("a raw check would wrongly defer opening the settings window when out of combat"). MayBeInCombat is the wrapper that defers on secret; IsInCombatSafe is the one that does not. The comment describes IsInCombatSafe's behaviour and the code calls MayBeInCombat.

> **Korrektur aus dem Widerlegungs-Durchgang:** The defect is the false-promise path (message printed + Toggle→Close + no pending flag), which reproduces in ordinary combat with no secret involved. The 'amplification' paragraph is NOT proven and should be dropped: wow-ui-source 12.1.0 (69299) RestrictedActionsDocumentation.lua:44-53 declares InCombatLockdown with a plain `bool` return and NO SecretArguments/SecretReturns annotation, so the premise in the code comments (main_frame.lua:376-379, slash_ui.lua:25-28) that it returns a secret-wrapped false inside instances is itself unsupported — today MayBeInCombat and IsInCombatSafe behave identically. The comment/code contradiction is real (the comment describes IsInCombatSafe's policy while the code calls MayBeInCombat) but it is currently latent. Correct fixes: have slash_ui print the deferral message only when main.Open actually returns false, or make Toggle-on-shown skip the gate entirely.

### 9. Every Settings-UI write swallows its apply error in a bare pcall — layers 1 and 3 of the fault isolation never see it

- **Flaeche:** ui-settings · **Schwere:** high · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_general.lua:50-52 and ui/page_channels.lua:44-46 (`if ns.ApplySettingChange then pcall(ns.ApplySettingChange, k) end`)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/safe_callback.lua:75-94 (_registerError: counts + routes to geterrorhandler); contrast core.lua:2594-2612, the /dilvl layout branch, which runs the same refresh chain UNPROTECTED so a throw surfaces normally
- **Unsere unausgesprochene Annahme:** That wrapping the widget callback in safe.WrapScript (widgets.lua:97,167,216,316) is enough. It is not: the wrapped callback calls setKey, and setKey's own bare pcall absorbs the throw before SafeCallback's pcall can ever see a failure. So the apply error is invisible to the per-page counter (safe_callback.lua:77), to the MAX_PAGE_ERRORS disable (safe_callback.lua:85-93), and to geterrorhandler/BugSack (safe_callback.lua:80-84). Every other error path in the UI does route to BugSack: main_frame.lua:449-452, blizzard_settings.lua:90-93, preview_widgets.lua:346-351, safe_callback.lua:80-84. These two lines are the only exception, and they sit on the single hottest path in the UI — every checkbox, radio, slider and dropdown write goes through them.
- **Fehlerszenario:** User clicks Layout → Columns on the General tab. setKey writes db.layout="columns" (page_general.lua:45), then ApplySettingChange("layout") runs core.lua:1760-1766: ClearAllBarTags() wipes every inline tag, HookAllBars() and RebuildNameIlvlMap() run, then RefreshAllColumns() throws. Result: db.layout="columns" is written and persisted to SavedVariables, all inline iLvl tags are gone, no column text is drawn, and NOTHING is reported — pcall discards the error object entirely (it is not even assigned). /dilvl debug prints "Details!-HookErrors: 0/5" (core.lua:2074-2077) and safe.GetDiagnostics() prints "no errors this session" (safe_callback.lua:154-156). The identical failure triggered via /dilvl layout columns IS unprotected (core.lua:2602-2606) and lands in BugSack. Same user intent, two entry points, one reports and one hides.

> **Korrektur aus dem Widerlegungs-Durchgang:** The pcall itself is defensible (a core throw must not kill the settings window); what is wrong is that the error object is discarded instead of forwarded. `local ok, err = pcall(...); if not ok then ns.ui.safe._registerError(ns.ui.safe.currentPage, 'apply:'..k, err) end` restores both the counter and the BugSack route while keeping the isolation.

### 10. "Reset to Defaults" never resets blizzDM, and VALIDATORS.blizzDM is dead code — because blizzDM is not a key in ns.defaults

- **Flaeche:** ui-settings · **Schwere:** high · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:1686 (`for k in pairs(defaults) do db[k] = nil end`) and core.lua:1654 (`for k, defaultVal in pairs(defaults) do` in ValidateDb)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/init.lua:23-39 — the defaults table; blizzDM is deliberately absent, documented as a comment on lines 37-38 instead of a key
- **Unsere unausgesprochene Annahme:** That iterating pairs(defaults) covers every setting. blizzDM is a tristate whose default IS nil, so it was written as a comment rather than a key. Both the wipe loop and the validator loop are keyed off pairs(defaults), so blizzDM falls out of both — the VALIDATORS.blizzDM entry at core.lua:1624-1628 can never execute, and the reset can never clear the key. This also makes blizzDM=auto reachable ONLY from the UI dropdown (page_channels.lua:91), because /dilvl blizzdm (core.lua:2546-2550) walks nil→true→false→true and never returns to nil.
- **Fehlerszenario:** User with Details! installed runs /dilvl blizzdm once → db.blizzDM = true (Forced On, core.lua:2547). Later they open Settings → Diagnostics → "Reset to Defaults" and confirm the popup, whose text reads "Reset ALL settings to their default values?" (locales/enUS.lua:104). ns.ResetToDefaults (core.lua:1677-1709) nils and re-fills every key in defaults — but db.blizzDM stays true. The Blizzard-DM overlay keeps rendering, the Output Channels dropdown still reads "Forced On" instead of "Auto", and the user has no way to tell why. Second effect: a SavedVariables file containing blizzDM = "off" (hand-edited, or a future migration bug) survives ValidateDb untouched; core.lua:2002 and page_channels.lua:85-87 both read the string as truthy, so a value that literally says "off" is reported and applied as Forced ON.

> **Korrektur aus dem Widerlegungs-Durchgang:** One side claim needs softening: /dilvl blizzdm CAN return to nil/auto, but only through the auto-disable recovery branch at core.lua:2542-2543 (db.blizzDM = prior). The normal toggle at 2546-2550 is indeed nil→true→false→true and never reaches nil, so from a healthy state the UI dropdown (page_channels.lua:90-95) is the only route back to auto. Fix: add `blizzDM = nil` semantics explicitly — e.g. a separate RESETTABLE_KEYS list, or wipe `for k in pairs(VALIDATORS)` in addition to pairs(defaults).

### 11. ns._pendingPages is written but never read — the only page-registration fallback in the addon is dead, and losing one page is invisible

- **Flaeche:** ui-settings · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_general.lua:167-171 (`ns._pendingPages = ns._pendingPages or {}; table.insert(...)`). Grepped every .lua in the addon: this is the ONLY occurrence of the identifier — no drain, no reader.
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/main_frame.lua:32-37 (RegisterPage, the only thing that populates main.pages) and main_frame.lua:444-455 (the PLAYER_LOGIN sanity check, which fires only when #main.pages == 0)
- **Unsere unausgesprochene Annahme:** That the queued entry gets replayed once main_frame loads. Nothing replays it. Worse, page_channels.lua:292-294, page_diagnostics.lua:216-218 and page_whatsnew.lua:118-120 have no fallback at all — they simply skip registration. And the sanity net only trips on total loss, so a partial loss produces no message.
- **Fehlerszenario:** TOC order changes so ui/page_general.lua loads before ui/main_frame.lua (a one-line reorder in Details_iLvlDisplay.toc, or a future split of main_frame into two files). page_general takes the else branch, pushes {"general","General",page.Init} into ns._pendingPages, and returns. Nothing ever reads that table. /dilvl opens a settings window with three tabs (What's New / Output Channels / Diagnostics) and no General tab — so the master Enable toggle, Color by gear tier, Show 2P/4P set bonus, Layout and Position have no UI at all. #main.pages is 3, not 0, so the PLAYER_LOGIN check at main_frame.lua:447 stays silent. No error, no BugSack entry, no chat message.

> **Korrektur aus dem Widerlegungs-Durchgang:** Downgrade the failure scenario: it is not reachable by any runtime input today. Details_iLvlDisplay.toc loads ui\main_frame.lua at line 28 and the four page files at lines 29-32, so page_general's else branch is currently unreachable dead code. The real defect is a non-functional safety net that reads as 'handled' — a maintainer who reorders the TOC or splits main_frame.lua will trust the fallback, get a settings window silently missing the General tab (master Enable, Color by gear tier, set bonus, Layout, Position), and hit no error, no BugSack entry and no chat message because #main.pages is 3, not 0. Fix: drain ns._pendingPages at the end of main_frame.lua (or delete the branch and change the sanity check to assert an expected page count rather than zero).

### 12. Details! Window dropdown cannot display values 6-10 — and its own tooltip tells the user to set them

- **Flaeche:** ui-settings · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_channels.lua:102-109 (DETAILS_WINDOW_OPTIONS holds only 0-5) and ui/widgets.lua:225-236 (syncDropdown calls UIDropDownMenu_SetText only from inside the `if choice.value == v` match loop)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:1868 (`if n and n >= 1 and n <= 10`) and core.lua:1601-1606 (VALIDATORS.detailsWindowId clamps to 0-10, so 6-10 survive the load-time validator); locales/enUS.lua:78 — TOOLTIP_DETAILS_WINDOW reads "Slash: /dilvl details window <all|1-10>"
- **Unsere unausgesprochene Annahme:** That every persisted detailsWindowId has a matching entry in the choices array. widgets.lua:225-236 has no else branch: when the getter returns a value not present in `choices`, UIDropDownMenu_SetText is never called at all, so the control keeps whatever text it had — which for a freshly created UIDropDownMenuTemplate is nothing. The page's own comment at page_channels.lua:98-101 acknowledges the 6-10 gap but only for the slash direction.
- **Fehlerszenario:** Power user runs /dilvl details window 7 (core.lua:1868-1871 accepts it, db.detailsWindowId = 7, iLvl now shows only on Details! window 7). They open Settings → Output Channels. syncDropdown (widgets.lua:226) gets v=7, calls UIDropDownMenu_SetSelectedValue(dd, 7), then loops the six choices, matches none, and returns without SetText — the dropdown renders blank where "Window 7" should be. Opening the menu shows six entries with none checked (info.checked = (7 == 0..5) is false for all, widgets.lua:215). Picking any entry to "fix" the blank silently discards the window-7 setting. Nothing throws.

> **Korrektur aus dem Widerlegungs-Durchgang:** The symptom is described wrongly — it is not blank, and this matters because 'blank' would read as a rendering glitch while the real output is a plausible-looking wrong label. Blizzard's UIDropDownMenu_SetSelectedValue (wow-ui-source .../Blizzard_SharedXML/Mainline/UIDropDownMenu.lua:948-954) calls UIDropDownMenu_Refresh, which at :896-899 does `if somethingChecked == nil then UIDropDownMenu_SetText(frame, VIDEO_QUALITY_LABEL6)`. VIDEO_QUALITY_LABEL6 = "Custom" (BlizzardInterfaceResources/Resources/GlobalStrings/enUS.lua:23140) / "Benutzerdefiniert" (deDE.lua:23140). So the control reads "Custom" where "Window 7" belongs. Fix: extend DETAILS_WINDOW_OPTIONS to 10, or give syncDropdown an else branch that renders the raw value.

### 13. Two different settings share one label AND one tooltip — the Output Channels checkbox claims to be the addon master switch but only toggles Details! bars

- **Flaeche:** ui-settings · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_general.lua:87-92 (label L["Enable Details! iLvl Display"], tooltip L["TOOLTIP_MASTER_ENABLE"], bound to db.enabled) vs ui/page_channels.lua:129-132 (identical label, identical tooltip, bound to db.showInDetails)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/locales/enUS.lua:34-35 — the shared tooltip text is "Master switch for the entire addon. Disable to silence every output channel without uninstalling." (deDE.lua:31-32 same); core.lua:1719-1737 shows the two keys drive completely different refresh paths
- **Unsere unausgesprochene Annahme:** That the label/tooltip pair uniquely identifies a setting. It does not: two independent db keys reuse both strings verbatim. As a side effect the safe-callback error labels collide too — widgets.lua:97 builds "Checkbox:OnClick:" .. label, so both produce the identical string "Checkbox:OnClick:Enable Details! iLvl Display" and safe.GetDiagnostics() (safe_callback.lua:144-158) cannot tell the two pages' checkboxes apart in a bug report.
- **Fehlerszenario:** User wants to silence the addon everywhere while raid-testing. They open Settings → Output Channels → Details! bars panel, hover the first checkbox, read "Master switch for the entire addon. Disable to silence every output channel without uninstalling.", and uncheck it. setKey("showInDetails", false) → ApplySettingChange("showInDetails") → ClearAllBarTags() only (core.lua:1731-1737). ElvUI [dilvl] tags, the Grid2 dilvl status, the Danders overlay and the Blizzard-DM overlay all keep rendering iLvl. If they then switch to the General tab, the identically-labelled checkbox there is still checked — same label, opposite state, on two tabs of the same window.

### 14. Slash writes never re-sync open UI widgets or the live preview — the first click on a stale checkbox is a silent no-op

- **Flaeche:** ui-settings · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:1822-1841, 2456-2612 — every slash branch writes db directly and calls the refresh functions; grepped MarkDirty across the addon: call sites are only ui/page_general.lua:54-56, ui/page_channels.lua:47-49 and core.lua:1701-1703. Zero slash branches call it.
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/widgets.lua:89-99 (checkbox re-reads the getter only in OnShow), :158-165 (slider), :237 (dropdown), :333 (radio group) — and ui/main_frame.lua:126-161, where SwitchTab is the only thing that re-runs a page's Init
- **Unsere unausgesprochene Annahme:** That db is the single source of truth and the widgets track it. They do not: a widget samples the getter once at creation and once per OnShow, and OnShow does not fire while the tab stays active. The comment at core.lua:1712-1716 claims ApplySettingChange exists so "the visual side-effects happen consistently regardless of which entry point changed the setting" — but the slash branches mostly bypass ApplySettingChange entirely, and none of them touch the UI widgets or the preview pane.
- **Fehlerszenario:** Settings window open on the General tab, db.enabled = true, the Enable checkbox is checked. User types /dilvl off in chat: core.lua:1827-1831 sets db.enabled = false and clears the bars. The checkbox on screen still shows CHECKED and the preview pane still shows tagged mock bars. User clicks the checkbox intending to turn the addon back on — UICheckButtonTemplate toggles it to UNCHECKED first, then widgets.lua:98 runs setter(self:GetChecked() and true or false) = setter(false) → db.enabled = false again, ClearAllBarTags(). The click that was meant to re-enable produced "still disabled". Two clicks are required and the first appears to do nothing. Same shape for /dilvl layout columns leaving the Layout radio on "Inline" and the DETAILS_SIZE_INLINE_NOTE (page_channels.lua:164-174) still visible.

### 15. detailsFontSize slider reads "12" while the stored value is 0 (auto) — a false readout on every fresh install

- **Flaeche:** ui-settings · **Schwere:** medium · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_channels.lua:155 (`function() local v = getKey("detailsFontSize", 0); return (v and v > 0) and v or 12 end`) feeding ui/widgets.lua:150-165 (syncTo → setLabel(v) at :155)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:483-486 (`local override = db and db.detailsFontSize or 0; if override > 0 then size = override end` — 0 means "copy Details' own bar font") and init.lua:35 (default detailsFontSize = 0)
- **Unsere unausgesprochene Annahme:** That a synthesized display value is harmless because isSyncing (widgets.lua:150,168) stops it being written back. The write-back is indeed blocked — but nothing stops it being SHOWN as fact. widgets.lua:138-139 renders the label as "<label>:  <n>" from the raw getter return, so the slider asserts a concrete point size that the addon is not using. The slider's own min is 6 (page_channels.lua:154), so 0 is not expressible in the control at all.
- **Fehlerszenario:** Fresh install, db.detailsFontSize = 0, db.layout = "columns", Details! bar font is 9pt. User opens Settings → Output Channels. The slider label reads "Details! Font Size:  12" and the thumb sits at 12, but CopyBarFont (core.lua:481-487) takes the override≤0 branch and renders the iLvl columns at 9pt. The user sees a stated 12 and a rendered 9 with no discrepancy indicated. If they nudge the slider off 12 and back to 12, db becomes 12 and the columns jump 9→12 — a visible change from a control that never moved. Returning to auto requires /dilvl details size 0; the tooltip (locales/enUS.lua:80) documents it, but nothing in the control does.

> **Korrektur aus dem Widerlegungs-Durchgang:** Minor citation fix: the tooltip that documents '/dilvl details size 0' is enUS.lua:81 (TOOLTIP_DETAILS_SIZE); line 80 is L["Details Font Size"]. Fix: extend the slider range to include 0 and special-case the label to render 'auto' when the stored value is 0, rather than synthesizing 12.

### 16. ns.GetDebugLines is declared the single source of truth for the debug dump but has no caller; the UI's Diagnostics dump silently omits blizzDM=auto and every counter /dilvl debug carries

- **Flaeche:** ui-settings · **Schwere:** low · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/page_diagnostics.lua:119-120 (`-- Export so the slash handler can reuse it as a single source of truth.` / `ns.GetDebugLines = collectDebugLines`) and :45-48 (`if db[k] ~= nil then addKV(k, db[k]) end`)
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/core.lua:1929-2361 — the /dilvl debug branch builds a completely separate, much larger dump and never references ns.GetDebugLines (grepped: the assignment at page_diagnostics.lua:120 is the only occurrence in the addon); core.lua:2002 prints blizzDM as `AUTO(on/off)`
- **Unsere unausgesprochene Annahme:** That the two dumps stay in step because one delegates to the other. They never did. The page's header (page_diagnostics.lua:3-4) tells users this text is for "CurseForge / GitHub bug reports", so it is what a user will actually paste.
- **Fehlerszenario:** User hits the v1.5.3 class of bug (iLvl missing on Details! bars). They open Settings → Diagnostics → Refresh → copy the text into a CurseForge ticket. The pasted report is missing: the resize-hook stats that exist specifically to catch the baseframe/baseFrame regression (core.lua:2042-2044), the cache/nameMap/short-form counts that identified the realm-stripping bug (core.lua:2036-2037), secretStats (core.lua:2078-2082), the per-callback error counters (core.lua:2085-2099), the BlizzDM per-entry table and the column diagnostics. And because line 48 skips keys whose value is nil, a user in blizzDM AUTO mode produces a report where blizzDM is absent entirely — indistinguishable from "the dumper does not know about that key".

> **Korrektur aus dem Widerlegungs-Durchgang:** Note the partial mitigation so the fix is scoped right: the window footer does point at the richer dump (L["FOOTER_HINT"] = "/dilvl debug for bug reports", enUS.lua:109). The fix is to make the slash branch actually call ns.GetDebugLines (as its comment claims) or to have the page render the slash dump, plus print explicit 'nil/auto' for tristates instead of skipping nil keys.

### 17. /dilvl ui <tab> closes the window instead of switching tabs when it is already open, and an unknown tab id silently opens What's New

- **Flaeche:** ui-settings · **Schwere:** low · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/slash_ui.lua:17,32 (parses a tab id, then calls main.Toggle(tab)) and ui/main_frame.lua:415-421 (Toggle discards pageId on the shown branch), :400-404 (unknown target falls through to main.pages[1])
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/Details_iLvlDisplay.toc — page files load in the order page_whatsnew, page_general, page_channels, page_diagnostics, so main.pages[1] is "whatsnew"
- **Unsere unausgesprochene Annahme:** That Toggle is the right verb for a command that carries a target. A request naming a destination is not a toggle. And main.Open's fallback silently substitutes a different page rather than telling the user the id was wrong.
- **Fehlerszenario:** Window is open on Diagnostics. User types /dilvl ui channels expecting the Output Channels tab. main.Toggle sees IsShown() and calls Close() — the window disappears, no message about the requested tab. A second /dilvl ui channels then opens it correctly. Separately, /dilvl ui chanels (typo) resolves main.pagesByID["chanels"] to nil (main_frame.lua:400) and opens the What's New tab with no "unknown tab" feedback, so the typo reads as a bug in the addon.

> **Korrektur aus dem Widerlegungs-Durchgang:** Worth flagging that this compounds finding 12: the fallback Open('chanels') also stamps state.lastSeenVersion at main_frame.lua:399, so a mistyped tab id both lands on the wrong page and burns the one-shot What's New for that character.

### 18. main.Open stamps lastSeenVersion unconditionally, so any explicit-tab open after an update consumes the one-shot What's New

- **Flaeche:** ui-settings · **Schwere:** low · **Still (keine Fehlermeldung):** True
- **Unser Code:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/main_frame.lua:392-399 — the whatsnew decision is inside `if not target then`, but `if state then state.lastSeenVersion = ns.version end` at :399 sits outside it
- **Gegenstelle:** e:/dev/gaming/wow-addons/Details_iLvlDisplay/ui/main_frame.lua:388-391 — the comment states the intent: "otherwise show 'What's New' once after the addon updates (so a new feature never goes unnoticed)"
- **Unsere unausgesprochene Annahme:** That every Open call is the one that would have shown the What's New tab. It is not — main.Open is also reached with an explicit pageId from slash_ui.lua:32 (/dilvl ui general) and with no pageId from the Blizzard settings panel button (blizzard_settings.lua:43). The version stamp is applied on all of them.
- **Fehlerszenario:** Addon updates from 1.5.5 to 1.5.6. Before opening the UI for the first time, the user types /dilvl ui channels to change a Danders setting. main.Open("channels") takes the `target` branch, never evaluates the whatsnew condition, and still writes state.lastSeenVersion = "1.5.6" at line 399. Every later bare /dilvl now takes the else branch at :396 and restores the last tab. The What's New page for 1.5.6 is never shown to that character, and there is no way to bring it back short of editing SavedVariables.

## Synthese des Laufs

# Details_iLvlDisplay — v1.5.6 Worklist

**Beweislage vorab (gilt für alles unten):** Alles ist **Quelltext-Analyse ohne laufenden Client** — kein Punkt wurde in-game beobachtet. Zwei Dinge sind dateibasiert *belegt*: (a) der installierte Addon-Ordner ist eine Junction auf `e:/dev/gaming/wow-addons/Details_iLvlDisplay`, es gibt also keine Installed-vs-Repo-Divergenz; (b) Grid2 ist auf diesem Account **per Charakter deaktiviert** (`WTF/Account/GAZAROTH/Blackhand/Baboroth/AddOns.txt`, 13.08. 22:45: `Grid2: disabled`) — der Grid2-Kanal lief hier noch nie, deshalb sind #5/#6 nie aufgefallen. Peer-Zeilen stammen aus den **installierten** Kopien (Grid2 4.0.8, ElvUI-Symlink, wow-ui-source 12.1.0/69299).

---

## 0. Voraussetzung: `/dilvl debug` messbar machen (~30 min) — zuerst bauen

Ohne das lässt sich unten fast nichts verifizieren: der Dump zeigt heute nur **Flags**, nie den **tatsächlich gerenderten Output** eines Kanals.

- **0a — UI-Fehlerzähler in den Slash-Dump hängen.** `ui/safe_callback.lua:142` behauptet „called by /dilvl debug", der einzige Aufrufer ist aber `ui/page_diagnostics.lua:101`. In `core.lua` nach `:2099` einfügen:
  `if ns.ui and ns.ui.safe then for _, l in ipairs(ns.ui.safe.GetDiagnostics()) do print(l) end end`
- **0b — Render-Probe pro Kanal.** In `elvui_tags.lua` `ns.dbg = ns.dbg or {}; ns.dbg.elvui = buildIlvl` exportieren, in `grid2_status.lua` `ns.dbg.grid2 = function(u) return DiLvl:IsActive(u), DiLvl:GetText(u), DiLvl:GetCount(u), DiLvl:GetColor(u) end`. Im Dump eine Zeile:
  `Rendered(player): elvui=%q  grid2 active=%s text=%q count=%s color=%s`
  Damit werden #1, #5, #6, #8 zu einer einzigen Dump-Zeile statt zu „schau auf den Frame".
- **0c — `ns._uiOpenPending` mitdumpen** (eine Zeile) → macht #2 diagnostizierbar.
- **0d — Host-Erkennung ehrlich machen.** `core.lua:2013-2015`, `core.lua:2467-2468`, `ui/page_diagnostics.lua:59`, `ui/page_general.lua:66` schließen alle aus `Grid2 ~= nil` auf „installiert". `C_AddOns` ist erreichbar (wird in `ui/page_diagnostics.lua:61-62` schon benutzt) → `absent` / `installed but disabled` / `loaded` unterscheiden.
- **0e — Tristates nie überspringen.** `ui/page_diagnostics.lua:48` (`if db[k] ~= nil`) lässt `blizzDM = nil` komplett aus dem UI-Report fallen — ununterscheidbar von „Key unbekannt". Explizit `auto` drucken. Grundsatzentscheidung dazu: entweder `ns.GetDebugLines` (`ui/page_diagnostics.lua:120`, **hat keinen Aufrufer**) vom Slash-Branch nutzen lassen, oder die Diagnostics-Seite den Slash-Dump rendern lassen. Zwei Dumps mit unterschiedlichem Inhalt sind der Grund, warum gepastete Bug-Reports die Resize-Hook-Stats (`core.lua:2042-2044`) und die Short-Form-Zahl (`core.lua:2036`) nicht enthalten — also genau die zwei Zeilen, die 1.5.3 und 1.5.4 aufgeklärt haben.

---

## P0 — silent + erreichbar + billig

### 1. Master-Switch `db.enabled` wird von ElvUI, Grid2 **und Danders** ignoriert
**Wirkung:** `/dilvl off` (`core.lua:1827-1831`) bzw. der General-Haken (`ui/page_general.lua:87-91`) ruft `NotifyElvUI()`; unsere Callbacks laufen, sehen `elvuiTag`/`grid2Status`/`dandersText` weiterhin `true` und **rendern die Zahl neu**. Kein Fehler, kein Hinweis.
**Unsere Zeilen:** `elvui_tags.lua:39` · `grid2_status.lua:32` und `:41` · **`danders_integration.lua:176`** (`if not db.dandersText then clearText(frame) return end` — kein `db.enabled`; vom Review nicht genannt, von mir nachgeprüft; `danders_integration.lua:339` hängt `refreshAll` am selben Bus).
**Vertragsbeleg:** `locales/enUS.lua:35` = „Master switch for the entire addon. Disable to silence **every** output channel"; `ui/preview_widgets.lua:219-231` blankt das Mock-Unitframe bei `db.enabled == false`, Caption `locales/enUS.lua:95` „Grid2 / ElvUI render analogously" plus Kommentar `ui/preview_widgets.lua:255-257`. Alle anderen Kanäle gaten korrekt: `blizzdm.lua:340`, `blizzdm.lua:929`, `core.lua:765`, `:806`, `:965`, `:1010`.
**Fix:** je ein `db.enabled` in die drei Gates; zusätzlich `elvui_tags.lua:108` auf `(db and db.enabled and db.elvuiTag)` ändern, sonst greift die ON→OFF-Latch (`:104-116`) nicht und der letzte Flush bleibt aus.
**Bewusst NICHT ändern:** die Cache-Schreibpfade `core.lua:1359-1383`, `:1487-1512`, `:1252-1271` bleiben ungegatet — der Cache soll warm bleiben. Konsequenz sauber halten: *schreiben* immer, *rendern* nur mit `enabled`. (Der Review-Satz „friert für den Rest der Session ein" ist widerlegt — die Zahl bewegt sich weiter, nur unregelmäßig, weil `core.lua:1433`, `:1454`, `:1472` und der Ticker `:1010` aussteigen.)
**Verify:** Party-Member gecached, `/dilvl elvui on` → `/dilvl debug` zeigt (0b) `elvui="[284]"`. `/dilvl off` → `Addon: OFF` (`core.lua:2003-2004`) **und** `elvui=""`, `grid2 active=false`. Danders-Overlay muss im selben Zug leer sein (`Details_iLvlDisplay_DandersDebug`, `core.lua:2018-2022`).
**Status:** Analyse.

### 2. `/dilvl` im Kampf schließt das offene Fenster und verspricht ein Wiederöffnen, das nie kommt
**Wirkung:** Fenster offen, Boss-Pull, `/dilvl` → Chat sagt „Settings UI deferred until combat ends" (`ui/slash_ui.lua:30`), Fenster geht **zu** (`ui/slash_ui.lua:32` → `main.Toggle` → `main_frame.lua:416-417` → `Close()`), `Close()` setzt **kein** `ns._uiOpenPending`, also findet der Handler `main_frame.lua:430-436` nichts. Die Meldung war schlicht falsch.
**Fix (eins von beiden):** Meldung nur drucken, wenn `main.Open` tatsächlich `false` liefert (`main_frame.lua:380-383`) — oder Toggle-auf-sichtbar am Gate vorbeiführen. Zweitens: `BuildFrame` fasst nichts Geschütztes an (`CreateFrame("Frame", …)` auf UIParent, `ui/main_frame.lua:201`) — wenn das Gate bleibt, gehört dort `IsInCombatSafe` hin (`secrets.lua:141-145`, secret⇒false), nicht `MayBeInCombat` (`secrets.lua:147-151`, secret⇒true). Jede andere ungeschützte Aufrufstelle im Addon nutzt `IsInCombatSafe` (`core.lua:603, 1056, 1101, 1472, 1500`; `blizzdm.lua:311, 936, …`); `MayBeInCombat` ist für Schreibzugriffe in Details!-FontStrings reserviert (`core.lua:723, 842, 912`).
**Nicht mitnehmen:** die „im ganzen Instanz-Run unerreichbar"-Verschärfung. `wow-ui-source 12.1.0 RestrictedActionsDocumentation.lua:44-52` deklariert `InCombatLockdown` mit schlichtem `bool` und **ohne** `Secret*`-Felder; das eigene Secret-Radar (`e:/dev/gaming/_learnings/secret-watch/reports/2026-08-08.md:24`) sagt dasselbe. Heute verhalten sich beide Wrapper identisch — die Kommentare `ui/main_frame.lua:376-379` und `ui/slash_ui.lua:25-28` behaupten etwas Unbelegtes und sollten entschärft werden.
**Verify:** Dummy pullen, Fenster offen, `/dilvl` → Fenster schließt **ohne** Defer-Zeile. Dann im Kampf bei geschlossenem Fenster `/dilvl` → Zeile erscheint einmal, `/dilvl debug` zeigt (0c) `UIPending: <tab>`, nach `PLAYER_REGEN_ENABLED` öffnet es.
**Status:** Analyse; der Defekt reproduziert in ganz normalem Kampf, ohne jeden Secret-Bezug.

### 3. Jeder Settings-Schreibvorgang verschluckt seinen Apply-Fehler
**Wirkung:** `ui/page_general.lua:50-52` und `ui/page_channels.lua:44-46` machen `pcall(ns.ApplySettingChange, k)` **ohne** Rückgabewert. Wirft z. B. `RefreshAllColumns()` im Layout-Zweig (`core.lua:1760-1766`), dann ist `db.layout = "columns"` geschrieben und persistiert, alle Inline-Tags sind weg (`ClearAllBarTags()` lief schon), keine Spalte wird gezeichnet — und nichts meldet es: `SafeCallback` sieht Erfolg, der Seitenzähler bleibt 0 (`ui/safe_callback.lua:77`), `MAX_PAGE_ERRORS` greift nie (`:85-93`), BugSack bekommt nichts (`:80-84`). Derselbe Fehler über `/dilvl layout columns` ist **ungeschützt** (`core.lua:2594-2612`) und landet in BugSack. Gleiche Absicht, zwei Eingänge, einer lügt.
**Fix (pcall behalten, Fehlerobjekt weiterreichen):**
```lua
local ok, err = pcall(ns.ApplySettingChange, k)
if not ok then ns.ui.safe._registerError(ns.ui.safe.currentPage, "apply:" .. k, err) end
```
`ui/page_channels.lua` hat `local safe = ns.ui.safe` (Zeile 22), `ui/page_general.lua` **nicht** — dort erst importieren.
**Verify:** temporär `if k == "layout" then error("selftest") end` an den Anfang von `ns.ApplySettingChange` (`core.lua:1717`), dann General → Layout → Columns klicken. `/dilvl debug` muss (0a) `page[general]: 1/5 errors  last: [apply:layout] …` zeigen und BugSack einen Eintrag haben. Patch wieder raus.
**Status:** Analyse. Kein heute bekannter Wurf auf diesem Pfad — es ist die Blindstelle, nicht der Bruch.

### 4. „Reset to Defaults" setzt `blizzDM` nie zurück, `VALIDATORS.blizzDM` ist toter Code
**Wirkung:** `blizzDM` ist kein Key in `ns.defaults` (`init.lua:23-39`, bewusst als Kommentar `:37-38`, weil der Default `nil` ist). Beide Schleifen laufen über `pairs(defaults)` — `core.lua:1686` (Wipe) und `core.lua:1654` (ValidateDb) —, also fällt `blizzDM` aus beiden heraus. Nutzer drückt Reset (Popup-Text `locales/enUS.lua:104`: „Reset **ALL** settings"), das BlizzDM-Overlay bleibt an, das Dropdown steht weiter auf „Forced On". Zweiter Effekt: ein handgeschriebenes `blizzDM = "off"` überlebt ValidateDb und wird von `core.lua:2002` und `ui/page_channels.lua:85-87` als **truthy = ON** gelesen.
**Fix:** entweder eine explizite `RESETTABLE_KEYS`-Liste oder zusätzlich `for k in pairs(VALIDATORS) do db[k] = nil end` bzw. Validierung über `pairs(VALIDATORS)` statt `pairs(defaults)`.
**Nebenkorrektur:** „`/dilvl blizzdm` erreicht `nil` nie" stimmt nur für den Normalfall (`core.lua:2546-2550`, nil→true→false→true); über den Auto-Disable-Recovery-Zweig (`core.lua:2542-2543`) kommt `nil` sehr wohl zurück. Aus gesundem Zustand ist das UI-Dropdown (`ui/page_channels.lua:90-95`) trotzdem der einzige Weg nach „auto".
**Verify:** `/dilvl blizzdm` → `/dilvl debug` zeigt `BlizzDM: ON` (`core.lua:2002-2007`). Reset ausführen → muss `BlizzDM: AUTO(on/off)` zeigen. Zweiter Test: SavedVariables von Hand auf `blizzDM = "off"`, `/reload`, Dump darf nicht `ON` sagen.
**Status:** Analyse.

---

## P1 — Grid2-Kanal + Kanal-Wiederbelebung

### 5. Grid2-Textindikator mit „Show stack" rendert `0` statt der Ilvl
**Wirkung:** wir überschreiben `IsActive/GetText/OnEnable/OnDisable/UpdateDB/GetColor` (`grid2_status.lua:25-56`), aber **nicht** `GetCount`. Grid2s Prototyp liefert die **Zahl 0** (`Grid2/GridStatus.lua:27-30`), und `Text_OnUpdateS` prüft `if count then` (`Grid2/modules/IndicatorText.lua:188-201`) — `0` ist in Lua truthy → `SetText(0)`. Handler wird gewählt, sobald `dbx.stack` gesetzt ist (`IndicatorText.lua:311-312`); der Haken ist eine gewöhnliche GUI-Option (`Grid2Options/modules/indicators/IndicatorText.lua:124-135`, „Show stack"). `GetText` wird nie aufgerufen, nichts wirft.
**Wichtig zum Scope:** kein zweiter Status nötig — `dbx.stack` hängt am **Indikator** (`IndicatorText.lua:303/311`). Wer den Haken je gesetzt hat, sieht `0`.
**Fix:** `function DiLvl:GetCount() end` — und gleich `function DiLvl:GetCountMax() end` mit (`GridStatus.lua:32-35`, Default `1`, betrifft Bar-Indikatoren).
**Verify:** Grid2 für den Charakter aktivieren, `dilvl` auf einen Text-Indikator legen, „Show stack" anhaken. `/dilvl debug` (0b): `count=0` vor dem Fix, `count=nil` danach; der Frame zeigt danach `284` statt `0`.
**Status:** Analyse; auf diesem Rechner nie ausgeführt (Grid2 disabled).

### 6. `db.colorIlvl` ist auf Grid2 ein stiller No-Op
**Wirkung:** `grid2_status.lua:28` erbt `Grid2.statusLibrary.GetColor` — das ist eine **Konstante** aus `dbx.color1` und kann unit-abhängig gar nichts liefern (`Grid2/GridUtils.lua:220-223`), und unser Default setzt `color1` auf Weiß (`grid2_status.lua:65-68`). `GetText` (`:39-50`) gibt die nackte Zahl ohne `|c`-Escape zurück. Details!-Bars und der ElvUI-Tag (`elvui_tags.lua:51-55`) färben orange, Grid2 bleibt weiß; `/dilvl color` erzeugt byte-identischen Text. Die eigene Vorschau (`ui/preview_widgets.lua:250-252` + Caption `locales/enUS.lua:95`) verspricht das Gegenteil.
**Fix:** `GetIlvlColor` in `util.lua:57-64` gibt nur Escape-Strings zurück — also `U.GetIlvlColorRGB(ilvl)` (r,g,b) einführen und `GetIlvlColor` daraus bauen (eine Quelle, sonst driften die zwei Tabellen), dann `DiLvl:GetColor(unit)` echt implementieren. **Dazu in den Hint (`locales GRID2_HINT`, `ui/page_channels.lua:213-218`):** die Farbe greift nur, wenn der Nutzer `dilvl` zusätzlich auf den Sidekick `<indicator>-color` legt (`Grid2/modules/IndicatorText.lua:342-348`); sonst malt `TextColor_OnUpdate` im else-Zweig weiß (`:320-329`).
**Verify:** `/dilvl debug` (0b) `color=…` — muss mit `/dilvl color` umschalten; am Frame nur sichtbar, wenn der `-color`-Sidekick zugewiesen ist.
**Status:** Analyse.

### 7. Geparkte `elvui`/`grid2`-Callbacks lassen sich aus der UI nicht wiederbeleben — und „Grid2 not installed" ist falsch
**Wirkung:** `/dilvl elvui on` (`core.lua:2458`) und `/dilvl grid2 on` (`core.lua:2471`) rufen `RestoreCallback`; der UI-Haken geht über `ApplySettingChange`, dessen Zweige (`core.lua:1771-1774`) **nur** `NotifyElvUI()` rufen. Nach dem 5-Fehler-Park (`core.lua:2812-2821`) kann der Nutzer den Kanal über die Settings also nie zurückholen — und für Grid2 verweigert genau der einzige Rettungsweg den Dienst, solange `Grid2 == nil` (`core.lua:2467-2474`), während der UI-Haken (`ui/page_channels.lua:208-210`) ohne jede Host-Prüfung schreibt. Der `dandersText`-Zweig (`core.lua:1779-1781`) macht es richtig.
**Fix:** in `core.lua:1771-1774` je ein `Details_iLvlDisplayAPI:RestoreCallback("elvui"/"grid2")` ergänzen; `core.lua:2468` von „not installed" auf „installiert, aber im AddOn-Menü deaktiviert" umstellen (Grid2 ist real vorhanden: `Grid2/Grid2.toc:4` Version 4.0.8, `LoadOnDemand: 0`) — siehe 0d.
**Verify:** Callback künstlich parken (5× werfen lassen), dann UI-Haken aus/an → `/dilvl debug` `Callback errors:`-Zeile (`core.lua:2094-2098`) muss wieder 0 zeigen **und** (0b) `elvui=…` wieder Text liefern.
**Status:** Analyse; dass Grid2 hier deaktiviert ist, ist dateibasiert belegt.

### 8. ElvUI-Tag gibt `""` statt `nil` zurück → oUF-Literale werden nie unterdrückt
**Wirkung:** `[name] [dilvl<␣ilvl]` (Suffix-Syntax). oUF unterdrückt Prä-/Suffixe **ausschließlich** über den nil-Test in `CreateTagFunc` (`ElvUI_Libraries/Game/Shared/oUF/elements/tags.lua:707`), und `""` ist truthy → der Frame liest dauerhaft „Raza ilvl" ohne Zahl. Kontrakt: `tags.lua:16-18`.
**Fix:** `elvui_tags.lua:39, :42, :45` → `return nil` (genau wie `blizzdm.lua:340` es schon macht).
**Nicht mitnehmen:** die Leerzeichen-Variante `[name] [dilvl]`. `tags.lua:755` coerct `nil` ohnehin zu `''` vor `SetFormattedText` (`:759`), und `tags.lua:21-23` dokumentiert Literale zwischen Tags als immer sichtbar — das ist nicht unser Leerzeichen.
**Verify:** `[dilvl<␣ilvl]` in ElvUI → Party → Name-Text setzen, `/dilvl elvui off` → Text muss komplett verschwinden statt „Raza ilvl" zu zeigen.
**Status:** Analyse.

### 9. Zwei verschiedene Settings, ein Label **und** ein Tooltip
`ui/page_general.lua:87-92` (`db.enabled`) und `ui/page_channels.lua:129-132` (`db.showInDetails`) benutzen beide `L["Enable Details! iLvl Display"]` + `L["TOOLTIP_MASTER_ENABLE"]` — und der Tooltip-Text (`locales/enUS.lua:34-35`, `deDE.lua:31-32`) verspricht „silence **every** output channel", schaltet aber nur `ClearAllBarTags()` (`core.lua:1731-1737`). Nebeneffekt: `ui/widgets.lua:97` baut das Fehlerlabel aus dem Text, beide Checkboxen kollidieren zu `Checkbox:OnClick:Enable Details! iLvl Display` und sind im Bug-Report nicht unterscheidbar (`ui/safe_callback.lua:144-158`).
**Fix:** Channel-Panel auf `L["Details! bars"]` + eigenen Tooltip umstellen; für die Fehlerlabels Page-ID voranstellen. **Verify:** Tooltip lesen, dann `/dilvl debug` — beide Zähler müssen getrennte Labels führen. **Status:** Analyse.

### 10. Slash-Schreibvorgänge synchronisieren offene Widgets nicht → erster Klick ist ein No-Op
Widgets lesen den Getter nur bei Erzeugung und `OnShow` (`ui/widgets.lua:89-99`, `:158-165`, `:237`, `:333`); `OnShow` feuert nicht, solange der Tab aktiv bleibt (`main_frame.lua:126-161`, `SwitchTab` ist der einzige Re-Init). `MarkDirty` wird ausschließlich aus `ui/page_general.lua:54-56`, `ui/page_channels.lua:47-49` und `core.lua:1701-1703` gerufen — **kein einziger Slash-Zweig**. Nach `/dilvl off` (`core.lua:1827-1831`) steht die Checkbox weiter auf CHECKED; der Klick, der wieder einschalten soll, sendet `setter(false)`. Zwei Klicks nötig, der erste sieht wirkungslos aus. Gleiche Form bei `/dilvl layout columns` (Radio bleibt „Inline", `DETAILS_SIZE_INLINE_NOTE` bleibt sichtbar, `ui/page_channels.lua:164-174`).
**Fix:** `ns.ApplySettingChange` am Ende die aktive Seite neu rendern lassen (`SwitchTab(activeTabId)` + `MarkDirty`) und die Slash-Zweige konsequent über `ApplySettingChange` führen — der Kommentar `core.lua:1712-1716` behauptet das ohnehin bereits. **Verify:** Fenster offen auf General, `/dilvl off` → Haken muss sofort leer sein. **Status:** Analyse.

---

## P2 — falsche Anzeige, kosmetisch bis irreführend

### 11. `detailsFontSize`-Slider behauptet „12", während 0 (auto) gespeichert ist
`ui/page_channels.lua:155` synthetisiert 12; `ui/widgets.lua:138-139` rendert das als Tatsache. Gerendert wird aber Details!' eigene Größe (`core.lua:483-486`, Default `init.lua:35` = 0). Slider-Minimum ist 6 (`ui/page_channels.lua:154`), 0 ist im Control nicht darstellbar. Wer den Regler auf 12 und zurück bewegt, springt sichtbar von 9pt auf 12pt — durch ein Control, das sich nie bewegt hat.
**Fix:** Range auf 0 erweitern und das Label bei 0 als „auto" rendern. Tooltip-Zitat richtigstellen: `/dilvl details size 0` steht in `locales/enUS.lua:81` (Zeile 80 ist das Label). **Verify:** frische Installation, Panel öffnen, Label muss „auto" sagen; `/dilvl debug` `Details-bars`-Block dagegen halten.

### 12. Details!-Window-Dropdown zeigt bei 6-10 „Custom"
`DETAILS_WINDOW_OPTIONS` kennt nur 0-5 (`ui/page_channels.lua:102-109`), Slash und Validator erlauben 1-10 (`core.lua:1868`, `:1601-1606`), und der Tooltip wirbt aktiv damit (`locales/enUS.lua:79`). `syncDropdown` ruft `UIDropDownMenu_SetText` nur im Match-Zweig (`ui/widgets.lua:225-236`) — **nicht leer**, sondern Blizzards Fallback: `UIDropDownMenu_SetSelectedValue` → `UIDropDownMenu_Refresh` → `if somethingChecked == nil then SetText(VIDEO_QUALITY_LABEL6)` (`Blizzard_SharedXML/Mainline/UIDropDownMenu.lua:896-899, 948-954`), also „Custom" / „Benutzerdefiniert". Ein plausibel aussehendes falsches Label ist schlimmer als eine sichtbare Lücke, und der nächste Klick verwirft die Einstellung still.
**Fix:** Liste auf 10 erweitern **oder** `syncDropdown` einen else-Zweig geben, der den Rohwert rendert. **Verify:** `/dilvl details window 7`, Panel öffnen → muss „Window 7" zeigen.

### 13. `/dilvl ui <tab>` schließt statt umzuschalten; unbekannte Tab-ID landet still auf What's New
`ui/slash_ui.lua:17,32` übergibt ein Ziel an `main.Toggle`, das den `pageId` im sichtbaren Zweig wegwirft (`main_frame.lua:415-421`); unbekannte IDs fallen auf `main.pages[1]` (`:400-404`) = `whatsnew` (TOC-Reihenfolge). Ein Befehl mit Ziel ist kein Toggle. Verschärfend: der Fallback stempelt auch noch `lastSeenVersion` (`main_frame.lua:399`), ein Tippfehler verbrennt also zusätzlich den einmaligen What's-New (siehe 14).
**Fix:** bei vorhandenem `tab` immer `Open`/`SwitchTab`; unbekannte ID mit Meldung + Liste quittieren.

### 14. `main.Open` stempelt `lastSeenVersion` bedingungslos
Die What's-New-Entscheidung steckt in `if not target then` (`main_frame.lua:392-398`), der Stempel `:399` steht außerhalb. Wer nach dem Update zuerst `/dilvl ui channels` tippt (oder den Blizzard-Panel-Knopf nutzt, `ui/blizzard_settings.lua:43`), sieht die 1.5.6-Seite nie wieder — Rückweg nur über SavedVariables.
**Fix:** Stempel in den `not target`-Zweig ziehen. **Verify:** `lastSeenVersion` in SavedVariables auf `1.5.5` setzen, `/reload`, `/dilvl ui channels`, dann `/dilvl` → What's New muss kommen.

### 15. `ns._pendingPages` wird geschrieben und nie gelesen
`ui/page_general.lua:167-171` ist die **einzige** Fundstelle im gesamten Addon (gegrept) — kein Drain, kein Leser. Die anderen drei Seiten haben gar keinen Fallback. Heute nicht auslösbar: die TOC lädt `ui\main_frame.lua` (Zeile 28) vor den Seiten (29-32), der else-Zweig ist toter Code. Das Problem ist das **Sicherheitsnetz, das wie „erledigt" aussieht**: wer die TOC umsortiert oder `main_frame.lua` splittet, bekommt ein Fenster ohne General-Tab (Master-Enable, Farbe, Set-Bonus, Layout, Position) — und die Sanity-Prüfung schweigt, weil sie nur bei `#main.pages == 0` greift (`main_frame.lua:444-455`).
**Fix:** entweder `_pendingPages` am Ende von `main_frame.lua` leeren oder den Zweig löschen und die Sanity-Prüfung auf eine **erwartete Seitenzahl** umstellen.

### 16. `seenGuids` wird gesetzt, bevor der Name bekannt ist
`core.lua:349` (`seenGuids[guid] = true`) läuft vor `:350-351` (`SafeUnitName`). Liefert `UnitName` ein Secret, gibt `SafeUnitName` `nil` zurück (`secrets.lua:98-101`; API-Grund: `UnitDocumentation.lua:2382-2385`, `SecretWhenUnitNameIdentityRestricted`), es wird nichts gespeichert — und der Ex-Group-Fallback (`core.lua:365-366`) überspringt genau diese GUID, weil sie schon als „gesehen" gilt. Zeile 349 in den `if name then`-Block schieben, zwei Minuten.
**Ehrlich dazu:** die im Review behauptete Sichtbarkeit („Bar zeigt kein Tag") folgt nicht — in solchem Content ist Details!' Bartext selbst secret (`Details/classes/class_damage.lua:3365`), und unser SetText-Hook steigt vorher aus (`core.lua:810-822`). Auch unbewiesen: ob 12.1 auf `raidN`-Tokens wirklich secret-Namen bei gleichzeitig klarem GUID liefert. **Fix, weil er gratis ist — nicht, weil der Schaden belegt wäre.**

---

## Offene Fragen — nur im Client zu klären (bitte je ein `/dilvl debug` aus dem Live-Test anhängen)

1. **`UnitName`-Prädikat-Wechsel in 12.1.** `git diff c878310d8..31c7f7b9c -- UnitDocumentation.lua` zeigt den Wechsel von `SecretWhenUnitIdentityRestricted` auf das neue `SecretWhenUnitNameIdentityRestricted` (`SecretPredicatesDocumentation.lua:128-132`). Richtung („in PvP mehr oder weniger secret") ist **nicht** bestimmbar. Das ist relevant, weil alles GUID-basierte überlebt, der **Default-Inline-Pfad** aber namensbasiert ist (`core.lua:433-435`, `ResolveGUIDByName` `:2674, :2686`) und still leer liefe. **Test:** `/dilvl map` + `/dilvl debug` in einem Battleground; `nameMap … short-form` (`core.lua:2036`) und `UnitNameBlocked` (`:2078-2082`) sind die Anzeigen. `secrets.lua:19-28` und `:84-91` dokumentieren noch das 12.0.7-Modell und erwähnen die Änderung nirgends.
2. **`FontString`-Secret-Aspekte (BlizzDM-Kanal).** Ein-Minuten-Test: nach einem Kampf auf einer Bar, deren `GetText()` secret ist, `nameFS:SetText("XX")` — steht danach sichtbar „XX" da, ist `FontString:ClearText()` der Fix.
3. **`issecretvalue`/`issecrettable`/`hasanysecretvalues` aus getaintetem Kontext.** Alle drei sind `SecretArguments = "AllowedWhenUntainted"` (`FrameScriptDocumentation.lua:229-278`) — dieselbe Annotation, die `secrets.lua:19-28` als Hart-Ablehnung dokumentiert. Die gesamte Abwehrschicht (`secrets.lua:46-50`) hängt daran. Einziger unpcallter Aufruf: `blizzdm.lua:1172`.
4. **`GetInventoryItemID`** hat in `Blizzard_APIDocumentationGenerated` **keinen** Eintrag. `util.lua:137-138` macht ein rohes `itemID > 0` auf Fremddaten im nicht-pcall-geschützten `INSPECT_READY`-Handler (`core.lua:1375`); ein Wurf dort killt den Cache-Write (`:1382`) **und** das Aufräumen von `pendingInspect` (`:1402`) → 15s Queue-Stall pro Spieler. Zwei Zeilen tiefer ist `C_Item.GetItemInfo` sehr wohl pcall-gewrappt.
5. **`.time`-Guard-Asymmetrie.** `core.lua:1229-1231` behält bewusst Einträge mit nicht-numerischem `.time`, sechs Konsumenten rechnen aber roh damit (`core.lua:245, 257, 1147, 1263, 1906, 2339`). `:1147` steckt in `QueueGroupInspect`, das nur aus `C_Timer.After`-Callbacks kommt und **nicht** pcall-gewrappt ist — der Sweep stürbe bei jedem Versuch am gleichen Roster-Index. Erreichbar nur über handgeschriebene SavedVariables. Frage: wenn es am Purge einen Guard wert ist, warum nicht an `:1147`?
6. **Blizzard-DM Rangpräfix.** `blizzdm.lua:429, :448, :830` hartkodieren `^%d+%.%s*`; `DAMAGE_METER_SOURCE_NAME` / `DAMAGE_METER_SPELL_ENTRY_UNIT` liegen nicht im Klon, das Format ist also unbelegt. Bei abweichendem Trenner in einer Locale scheitert `ResolveFrameGUID` und lockt den Spieler nach `MAX_RESOLVE_FAILS = 3` in GAVE-UP.
7. **Mixin-Kopier-Timing.** `hooksecurefunc(DamageMeterEntryMixin, "UpdateName", …)` (`blizzdm.lua:981`) erreicht nur Frames, die **nach** unserem Load entstehen. Existiert je ein Entry-Frame vor unserem `ADDON_LOADED`, bleibt er dauerhaft ungehookt und untagged.
8. **`fs:SetParent()` im Kampf** (`danders_integration.lua:138`) — kein Präzedenzfall in DandersFrames; `CreateFontString` unter einem Secure-Parent (`:151`) ebenso ungeprüft. Braucht einen Kampf-Test mit `/console scriptErrors 1`.
9. **Grid2-Kanal insgesamt.** Auf diesem Account nie geladen (siehe Vorbemerkung) — vor dem Release einmal Grid2 aktivieren und #5/#6/#7 durchspielen; sonst geht 1.5.6 mit einem Kanal live, der weiterhin unbeobachtet ist.

## Offene Fragen / nicht pruefbar (45)

Enthaelt auch ausdrueckliche Positiv-Befunde ("VERIFIED CLEAN") — die sind Ergebnis, nicht Luecke.

- blizzdm: No installed Blizzard_DamageMeter exists on disk — E:/World of Warcraft/_retail_/Interface/AddOns contains 90 addons and zero Blizzard_* folders (Blizzard addons ship inside the game archive). Everything above is therefore checked against the reference clone e:/dev/gaming/wow-addons/wow-ui-source at tag 12.1.0 (build 69299), commit 31c7f7b9cc79e56c986b365c06a6afbcf3c9177b, dated 2026-08-13. If the live client is newer than that build, the mixin bodies are unverified.
- blizzdm: The installed Details_iLvlDisplay at E:/World of Warcraft/_retail_/Interface/AddOns/Details_iLvlDisplay is byte-identical to e:/dev/gaming/wow-addons/Details_iLvlDisplay (same .git directory, diff clean across all 9 lua/toc files) — it is the same directory via a junction. There is no installed-vs-repo divergence to report for this addon.
- blizzdm: Runtime behaviour of the FontString secret-aspect system. Finding 1 is derived from the generated API annotations (SecretArgumentsAddAspect / SecretReturnsForAspect / ChecksForbiddenAspects) plus the ClearText documentation string, NOT from observation. I could not run the client to confirm that (a) a plain-string SetText really does land on an aspect-marked FontString and (b) SetToDefaults really fails to clear the Text aspect. Both are testable in one minute in-game: after a fight, on a bar whose GetText() is secret, call nameFS:SetText("XX") and check whether the bar visibly reads XX. If it does, finding 1 is confirmed as written and FontString:ClearText() is the fix.
- blizzdm: DAMAGE_METER_SOURCE_NAME and DAMAGE_METER_SPELL_ENTRY_UNIT are GlobalStrings and are not present in the wow-ui-source clone, so I could not verify their literal format. blizzdm.lua:429, :448 and :830 all hard-code the rank prefix as `^%d+%.%s*` ("1. Name"). If any locale ships a different separator, the left-position insert degrades cosmetically and — more seriously — ResolveFrameGUID's nameText and FontString fallbacks stop matching, which after MAX_RESOLVE_FAILS = 3 latches that player into GAVE-UP. Unverified either way.
- blizzdm: Whether issecretvalue / issecrettable / hasanysecretvalues actually accept secret arguments from our tainted execution context. All three are annotated SecretArguments = "AllowedWhenUntainted" (FrameScriptDocumentation.lua:229-278) — the same annotation that secrets.lua:19-28 documents as the hard-reject class. It is also the default value across the docs (3566 of 3781 occurrences), so it most likely carries no restriction here, but the entire defense layer in secrets.lua:46-50 rests on it and I could not confirm at runtime. Only blizzdm.lua:1172 calls hasanysecretvalues outside a pcall.
- blizzdm: DamageMeterSpellEntryTemplate rows in the SourceWindow. They inherit DamageMeterEntryTemplate (DamageMeterEntry.xml:50) so our mixin-level UpdateName hook fires on them; they have no sourceName, so ResolveFrameGUID (blizzdm.lua:408-417) returns any cached _dilvlGUID unvalidated; our Init hook is on DamageMeterSourceEntryMixin only (blizzdm.lua:490) so it never refreshes them; and no strip path reaches the SourceWindow's own ScrollBox (it has a separate ForEachEntryFrame at DamageMeterSourceWindow.lua:205-209 that we never call). The structural gap is real, but I could not construct a spell-row nameText that ResolveGUIDByName actually resolves — DamageMeterSpellEntryMixin:GetNameText (DamageMeterEntry.lua:705-728) wraps player names in class colour codes that StripTagFromText does not remove — so I am reporting it as a question, not a finding.
- blizzdm: Mixin-copy timing. XML `mixin=` shallow-copies functions onto each frame at creation, so hooksecurefunc(DamageMeterEntryMixin, "UpdateName", ...) at blizzdm.lua:981 only affects frames created AFTER our load. Blizzard_DamageMeter is non-LoadOnDemand with LoadSavedVariablesFirst (Blizzard_DamageMeter.toc), and combat sessions do not persist across logout, so at login BuildDataProvider yields an empty provider and no entry frames should exist yet. I could not verify this ordering at runtime; if any entry frame is ever created before our ADDON_LOADED, that frame is permanently unhooked and silently untagged.
- details-core: Whether FontString:GetStringWidth()/GetText() actually return secret values for Details' 12.1 right-hand text during live combat. This is what decides how often finding 6's "measured nothing" case fires and whether the isSecretValue clamps at core.lua:591/612/625/638 turn real widths into 0. Source reading cannot settle it — it needs an in-game /dilvl debug during a pull.
- details-core: Whether format("%d. %s", rank, secretName) at Details/classes/class_damage.lua:3409 yields a secret string that reaches our SetText hook. Details itself branches on issecretvalue there (:3408) and then executes an identical format call in both branches, which suggests format tolerates secrets, but I could not prove the runtime type of the result. Our hook has the isSecretValue guard at core.lua:810 either way.
- details-core: openRaidLib.GetUnitID is called at Details/Libs/LibOpenRaid/LibOpenRaid.lua:1918 (and 13 other sites) but has NO definition anywhere in E:/World of Warcraft/_retail_/Interface/AddOns (the bundled copy is the only LibOpenRaid installed). If it is genuinely undefined, AddUnitGearList would throw rather than fire GearUpdate. Moot while our registration fails, but it means the gear callback path in the shipped lib is untested on this build.
- details-core: Whether Details.item_level_pool[guid].name can be a secret string on 12.1. core.lua:1904 reads it and the /dilvl cache path concatenates it into a print. That depends on Details:GetFullName (core/inspect.lua:146) and Details:IlvlFromNetwork (:75), which I did not trace to their secret behaviour. Outside the bar channel, so I did not chase it.
- details-core: How Details! behaves when a window is closed and reopened with respect to instance.baseframe identity — core/meta.lua:258-263 nils baseframe and barras at logout, and classes/class_instance.lua:2605 assigns a fresh frame in RestauraJanela, but I did not verify whether gump:CriaJanelaPrincipal recycles the named global frame. If it creates a new frame each time, hookedInstances (core.lua:62, strong keys) accumulates dead frames and resizeStats.installed over-counts. No functional impact found, so not reported as a finding.
- details-core: Field/method spellings were all verified as CORRECT against the installed source — no new case-sensitivity trap found: instance.baseframe (class_instance.lua:2508/2605), instance.barras (:2476/2584/2393), bar.minha_tabela (class_damage.lua:3619), bar.instance_id (window_main.lua:4210), bar.border/statusbar/lineText1-4 (window_main.lua:4265/4235/4373/4387), inst.row_info.text_yoffset (include_instance.lua:294), actor.serial/nome/displayName, actor:IsPlayer (mixin.lua:172), combat:GetContainer (class_combat.lua:169), container:ListActors (container_actors.lua:518), Details:GetInstance (class_instance.lua:1094), Details:GetCurrentCombat (container_segments.lua:39), Details.ilevel:GetIlvl (core/inspect.lua:560), Details.item_level_pool, Details.userversion/version (boot.lua:24/28), DETAILS_ATTRIBUTE_DAMAGE/HEAL (Definitions.lua:92-93).
- elvui-grid2: Whether UNIT_INVENTORY_CHANGED (declared at elvui_tags.lua:64 and :68) ever actually fires for a party/raid unit token. oUF binds it per-unit at ElvUI_Libraries/Game/Shared/oUF/elements/tags.lua:878-882 (`handler:RegisterUnitEvent(event, frame.unit)`), so if the event only fires for "player" the declared event is dead weight on every frame except the player frame and the tag depends 100% on our RefreshMethods bus. Proving the real firing behaviour needs a live client; I did not run one.
- elvui-grid2: The comment at elvui_tags.lua:96-98 claims E.oUF can be "momentarily nil (profile switch, ElvUI update)". I could not find support for that in the installed ElvUI: E.oUF is assigned once at ElvUI/Game/Shared/General/Initialize.lua:45 and never cleared, and nothing in ElvUI or ElvUI_Options wipes ElvUF.Tags.Methods (the only other reader is ConfigEnvironment.lua:189-193, which merely swaps fenvs for allowTags / name: / health: / power: tags - ours match none of those, so ElvUI test mode does not touch us). The pcall is harmless, but the stated reason is unverified; the comment may be documenting a symptom whose real cause was never found.
- elvui-grid2: Whether a user can reach the duplicate-status case in Grid2. Our CreateStatusDilvl (grid2_status.lua:58-61) ignores baseKey and always registers the same singleton, while Grid2:RegisterStatus keys by status.name (Grid2/GridStatus.lua:142-144). A second db entry with type="dilvl" under a different baseKey would overwrite DiLvl.dbx and then fail silently at Grid2/GridSetup.lua:62 (only a Grid2:Debug line). I could not find a GUI path to create one - we register no MakeNewStatusOptions entry - so I can only prove it for hand-edited SavedVariables or an imported profile, which is not a concrete user scenario.
- elvui-grid2: Grid2's unit-level Load filters (unitClass/unitRole/unitType/unitAlive/unitReaction/unitPlayer) require the status to test self.filtered[unit] inside IsActive - documented at Grid2/GridStatusLoad.lua:153-154 and implemented by e.g. Grid2/modules/StatusMana.lua:86,96,131. Our DiLvl:IsActive (grid2_status.lua:30-37) does not. I initially had this as a finding, then disproved reachability: Grid2Options/modules/statuses/Status.lua:357 gates that whole option block behind `optionParams.unitFilter`, which is only set via RegisterStatusOptions - we register none, so those toggles are not shown for `dilvl`. The general filters (class/spec/zone/group type) and the combat filter do work correctly with our status. Worth adding the guard defensively, but I cannot give a user-reachable failure scenario.
- elvui-grid2: Live behaviour of an ElvUI profile switch and a Grid2 layout/profile reload. Statically both look sound: Grid2's ProfileShutdown -> SetupShutdown (GridSetup.lua:9-18) unregisters our status and ProfileChanged -> LoadConfig -> Setup -> SetupStatuses (GridSetup.lua:45-55, :75-81) re-runs our setupFunc, and Grid2:DbSetStatusDefaultValue (Grid2/GridDefaults.lua:11-17) keeps the AceDB default so a brand-new profile also gets the dilvl entry. I verified this by reading, not by switching profiles in the client.
- danders: VERIFIED CLEAN — version drift: the installed Details_iLvlDisplay at E:/World of Warcraft/_retail_/Interface/AddOns/Details_iLvlDisplay is byte-identical to the working tree (recursive diff, no differences). The DandersFrames reference clone at e:/dev/gaming/wow-addons/DandersFrames/DandersFrames is v5.2.0-alpha.1, AHEAD of the installed v5.1.3 — but every file our integration depends on (Core/API.lua, Frames/Core.lua, Frames/Headers.lua, Frames/Create.lua, Libs/CallbackHandler-1.0) is byte-identical between the two. Only Locales, Changelog, Profiler, Migrations, LibDeflate and the .toc differ. No incoming API drift on our surface.
- danders: VERIFIED CLEAN — frame lifecycle: frames are recycled, never destroyed. InitializeHeaderChild is guarded by frame.dfInitialized (Headers.lua:564 / :877) and is the only caller of CreateFrameElements for header children (Headers.lua:616), so frame.contentOverlay is created exactly once per frame (Create.lua:684). Reuse for a different unit is an in-place write of self.unit (Headers.lua:733) — our FontString survives, only its text needs replacing, which is what ensureFS assumes. Nothing in DandersFrames re-parents, resizes, hides or re-creates contentOverlay after creation (grep over all .lua returned zero hits for contentOverlay:Hide/Show/SetParent/SetSize/SetPoint/SetAlpha/SetScale), and no code sweeps a frame's regions to remove foreign ones.
- danders: COULD NOT CHECK — is fs:SetParent() (danders_integration.lua:138) safe in combat? This is the ONE call in our OnFramesSorted path with no peer precedent. Evidence FOR safety: DandersFrames gates only frame:SetSize on header children behind InCombatLockdown (Core.lua:805-820) and explicitly documents the sibling frame.healthBar:ClearAllPoints()/SetPoint() at Core.lua:823-825 as "unprotected" and runs them in combat unconditionally — so laying out an insecure child of a SecureUnitButtonTemplate frame in combat is fine. But I found no in-combat SetParent anywhere in DandersFrames to compare against, and the AuraContainer combat gate (AuraContainer.lua:6045) is about the AuraContainer TEMPLATE, not about plain CreateFrame/CreateFontString under a secure parent (AuraContainer.lua:159-164 states the real reason). CreateFontString under a secure parent in combat (ensureFS:151) is likewise unproven either way. Both need a live in-combat test with /console scriptErrors 1.
- danders: COULD NOT CHECK — error visibility inside the OnFramesSorted dispatch. Libs/CallbackHandler-1.0/CallbackHandler-1.0.lua:15-22 (v8) dispatches via securecallfunction, NOT xpcall. Whether a throw from our refreshAll would surface to BugSack, be silently swallowed, or abort the remaining subscribers is not determinable from source. Latent today because refreshAll wraps everything in pcall and cannot throw — but it becomes the failure-visibility path the moment anyone adds an unwrapped statement to refreshAll.
- danders: NOT A DEFECT, BUT SILENT — pinned frames get no overlay. DandersFrames_IterateFrames -> IterateCompactFrames covers party, raid and arena only (Frames/Core.lua:588-618). Pinned frames are a separate set with their own API (Core/API.lua:614-653) and they DO carry frame.unit and frame.contentOverlay (Features/PinnedFrames.lua:3594 and :3602-3603). A user running pinned sets sees iLvl on their normal frames and nothing on the pinned ones, with no message.
- danders: CHECKED AND CLEARED — OnFramesSorted coverage of unit reassignment. The LEVEL 2 early return (Headers.lua:677-709, returns at :708) skips the callback fire, but it fires only when the frame keeps the SAME player under a new token, so our GUID-keyed text stays correct; and that situation always coincides with a roster change, which reaches us anyway via core.lua's GROUP_ROSTER_UPDATE callback. The unit-removal path (attribute -> nil) does reach the fire block, which sits outside the `if actualUnit` guard (Headers.lua:813-828 vs :830-852). The only gap is the LEVEL 1 skip described in finding 3.
- danders: CHECKED AND CLEARED — key-name and geometry assumptions. frame.unit and frame.contentOverlay are the exact spellings DandersFrames writes (Headers.lua:733, Create.lua:684 and :1082 — two creation sites, both identical). contentOverlay is SetAllPoints() on the frame with no padding inset, so our POS entries map 1:1 onto the frame's real corners; DF anchors its own nameText/healthText to `frame` rather than contentOverlay (Create.lua:697, :709) and gets the same geometry. The POS table is duplicated in three places (danders_integration.lua:101-118, init.lua:43-52, ui/preview_widgets.lua:161-175) — I diffed all three and they currently agree exactly, but that is three copies with no shared source and no test.
- secrets-taint: Direction of the 12.1 UnitName predicate swap. PROVEN: `git diff c878310d8(12.0.7) 31c7f7b9c(12.1) -- UnitDocumentation.lua` shows UnitName changed from `SecretWhenUnitIdentityRestricted` to a brand-new `SecretWhenUnitNameIdentityRestricted` (now UnitDocumentation.lua:2382-2384; predicate defined at SecretPredicatesDocumentation.lua:128-132, added in 12.1). Its rule is "regular unit identity secrecy rules, EXCEPT in PvP when the queried unit is a player" — I could NOT determine whether that exception makes UnitName MORE secret (PvP anonymisation) or LESS secret (UnitName already returns the modified/anonymised name in PvP, while UnitNameUnmodified kept the strict rule — which is the reading I lean to, but cannot prove). This matters because our code is asymmetric: everything GUID-keyed (ElvUI tag, Grid2, Danders, BlizzDM, the columns layout via bar.minha_tabela.serial) survives a nil name, but the DEFAULT inline Details!-bar path (BuildTag → nameToIlvl, core.lua:433-435) and ResolveGUIDByName's roster loop (core.lua:2674, :2686) are name-only and would go silently blank. secrets.lua:19-28 and :84-91 still document the 12.0.7 model (RequiresDeclassifiedUnitIdentity / the 67186→67235 flip-flop) and mention this change nowhere. Needs one live /dilvl map + /dilvl debug run inside a battleground to settle.
- secrets-taint: Whether issecretvalue / issecrettable / hasanysecretvalues actually accept a secret argument from tainted (addon) code. All three are annotated `SecretArguments = "AllowedWhenUntainted"` (FrameScriptDocumentation.lua:228-278) — the same annotation that on UnitGUID means "a tainted caller may not pass a secret". Their argument type is `LuaValueReference`, which I could not find a semantic definition for anywhere in the generated docs, and Blizzard's own uses (Blizzard_SharedXMLBase/Pools.lua:274, Blizzard_EventTrace.lua:796) are all untainted. If the annotation means literally what it says, the entire guard layer in secrets.lua would be self-defeating; the addon evidently works in practice, so it almost certainly does not — but I have no proof, only absence of contradiction.
- secrets-taint: Whether IsShown() can return a secret on the frames we read. SimpleScriptRegionAPIDocumentation.lua:531-543 and SimpleFrameAPIDocumentation.lua:914-926 give IsShown `SecretReturnsForAspect = {Enum.SecretAspect.Shown}`, and SetShown adds that aspect from a secret argument (SimpleScriptRegionAPIDocumentation.lua:655, SimpleFrameAPIDocumentation.lua:1485). We treat IsShown as a plain bool at core.lua:551, :605, :618, :631, :689, :725-726, :921, :1524, :2198, :2221 and blizzdm.lua:1408. A secret would not throw (it reads truthy), so `if not bar:IsShown()` would silently process hidden bars and `if fontString:IsShown()` would write to hidden ones. I could not find any Details! or Blizzard_DamageMeter call that passes a secret to SetShown, so I could not turn this into a failure scenario.
- secrets-taint: Whether GetInventoryItemID can return a secret. It has NO entry anywhere in Blizzard_APIDocumentationGenerated (legacy undocumented API), so its secrecy is unverifiable from the peer source. util.lua:137-138 does `local itemID = GetInventoryItemID(unit, slotID)` / `if itemID and itemID > 0 then` — a raw `>` on foreign data with no isSecretValue guard, on an INSPECTED unit token (core.lua:1375, inside the INSPECT_READY OnEvent handler, which is not pcall-wrapped). The C_Item.GetItemInfo call two lines below IS pcall-wrapped, so the author clearly considered that line risky and not this one.
- secrets-taint: No live in-game evidence was available. Every claim above is derived from source and annotations (wow-ui-source 12.1.0 build 69299, BlizzardInterfaceResources 12.1.0 build 69283, installed Details 20260811.15270.172, installed Blizzard_DamageMeter). I have no /dilvl debug or /dilvl taint dump from inside a restricted instance, so all of S.stats (unitNameBlocked / unitNameRejected / unitIsUnitBlocked) is unobserved — including whether the guards ever fire at all.
- secrets-taint: PEER-COPY DIVERGENCE, as required: our own addon is a SYMLINK (E:/World of Warcraft/_retail_/Interface/AddOns/Details_iLvlDisplay -> /e/dev/gaming/wow-addons/Details_iLvlDisplay), so working tree == installed, no divergence. ElvUI is likewise symlinked. Details is NOT: the reference clone e:/dev/gaming/wow-addons/Details-Damage-Meter is AHEAD of the installed copy (clone HEAD = c139bf36 "Rework how text objects work on bars", adds functions/rowtexts.lua and modifies class_damage.lua / class_heal.lua / class_custom.lua / window_main.lua / skins.lua; installed = 20260811.15270.172, no rowtexts.lua). That is the already-known unreleased row-text rework, so I analysed against the INSTALLED copy throughout and cited it explicitly (core/parser_nocleu1.lua, core/inspect.lua, classes/class_damage.lua line numbers are all from the installed copy). Details.ilevel:GetIlvl is byte-identical in both (core/inspect.lua:560-561, `return Details.item_level_pool[guid]` — confirming core.lua:266-268's description). Grid2 and DandersFrames are also real directories in AddOns rather than symlinks; I did not diff those two against their clones.
- secrets-taint: ANSWER TO THE PROTECTED-FRAME-IN-COMBAT QUESTION: no, we never write into a protected or secure frame. Proven: the only Blizzard-owned objects we write to are the DamageMeter entry Name FontStrings, and DamageMeterEntry.xml:3-51 declares plain <Button> templates with no `protected="true"` and no secure-template inheritance (grep for protected=/SecureHandler/SecureActionButtonTemplate/SetProtected across Blizzard_DamageMeter returns nothing). Details! bars are addon-created and therefore also unprotected. Consequence worth noting: the `IsProtectedFunction = true` APIs we call on them — SetPoint/ClearAllPoints (blizzdm.lua:507-512, core.lua:707-715), SetSize/SetWidth (core.lua:729, :751), SetToDefaults (blizzdm.lua:560) — carry no combat restriction on these objects, which means the `if not MayBeInCombat() then ... bar.lineText1:SetSize(...)` guard at core.lua:722-730 (and the matching lack of any guard in ClearAllColumns at core.lua:743-753) buys nothing and costs name-width clamping for the whole fight. One 12.1 change does reach these objects though: SetPoint/SetAllPoints gained `CheckAllowInheritForbiddenLayoutAspects` (SimpleScriptRegionResizingAPIDocumentation.lua:111-141) and ForbiddenAspectConstantsDocumentation.lua:16 defines `UntrustedLayoutScriptExecution` as propagating "to any children of this object, and anything anchored to this object" — our column FontStrings anchor to bar.statusbar (core.lua:708, :715) and our Danders FontStrings reparent onto frame.contentOverlay (danders_integration.lua:136-139), so both could inherit a host's forbidden layout aspect. I could not find a host that sets one, so this is a watch item, not a finding.
- data-edge: No in-game verification of anything. Every claim below and above is from source reading only — I could not launch WoW, so no scenario was reproduced live.
- data-edge: REACHABILITY GAP, reported as a QUESTION not a finding: the ADDON_LOADED purge at core.lua:1229-1231 deliberately KEEPS an entry whose `.time` is not a number ("treating unknown age as infinitely old is how the old bug destroyed data"), but six consumers then do raw arithmetic on that field — core.lua:245, :257, :1147, :1263, :1906, :2339. If such an entry existed, `time() - cached.time` at core.lua:1147 would throw inside QueueGroupInspect, which is only ever called from C_Timer.After callbacks (core.lua:1320/1323/1324/1445/1468/1484/1512) and is NOT pcall-wrapped — so the sweep would die at the same roster index on every attempt and no one after it would ever be inspected again. I could NOT prove the state is reachable: `git log -S` over core.lua shows every ilvlCache write since the initial commit (6ded1b5, 18e0a8c, a968cb3, a34aa3e, plus current :251/:290/:1172/:1267/:1382) includes `time = time()`, and MigrateSchema (core.lua:1642-1651) is an empty placeholder, so no old schema produced timeless entries. It is reachable only via a hand-edited SavedVariables file — which is exactly what ValidateDb's own header comment (core.lua:1204-1206) claims to defend against. Question for the maintainer: if a non-number `.time` is worth a guard at the purge, why is it not worth one at line 1147?
- data-edge: Could not determine whether LibOpenRaid can deliver a non-number or NaN `ilevel`. Our guard at core.lua:1257 (`gearInfo.ilevel <= 0`) throws on a string and lets NaN through to `math.floor` at :1261, which would put a NaN into ilvlCache and then into SavedVariables. The lib assigns the field with no type check from unpacked comm data (Details/Libs/LibOpenRaid/LibOpenRaid.lua:1892-1895, callers at :1325/:1801/:1953). Mitigating: LoR dispatches our callback under xpcall (LibOpenRaid.lua:962), so a throw is contained to one BugSack error and one lost update.
- data-edge: Could not verify whether GetInventoryItemID can return a secret value: it has NO entry in Blizzard_APIDocumentationGenerated at all (grep over the whole wow-ui-source Interface tree returns only call sites, no doc block), so its secret behaviour is undocumented rather than confirmed-safe. If it ever does return a secret, `itemID > 0` at util.lua:138 throws, and inside INSPECT_READY (core.lua:1375) that aborts the handler before the cache write at :1382 AND before pendingInspect is cleared at :1402 — stalling the inspect queue for the full 15s safety timeout per player.
- data-edge: Out of lens, not examined: blizzdm.lua, elvui_tags.lua, grid2_status.lua, danders_integration.lua and ui/* name handling (only their shared entry points Details_iLvlDisplayAPI.ResolveGUIDByName / StripRealm at core.lua:2671-2732 were read).
- data-edge: POSITIVE CONTROLS — checked against the named live data and found genuinely SAFE, so the absence of findings there is a result, not a gap: (1) No Lua pattern anywhere in the addon is built from a variable — a regex sweep for find/match/gmatch/gsub with a non-literal first argument over all .lua files returns zero hits — so realms with parentheses (Aggra(Portugues)) and apostrophes (Quel'Thalas) cannot cause pattern injection; they are only ever subjects or concatenation operands. (2) StripRealm's `^([^%-]+)` (util.lua:50) splits on byte 0x2D, which cannot occur inside a UTF-8 multi-byte sequence (lead bytes 0xC2-0xF4, continuations 0x80-0xBF), so Cyrillic and accented names cannot be cut mid-character; taking the FIRST segment also stays correct for hyphenated realms such as Azjol-Nerub. (3) ExtractName (util.lua:162-174) uses only %d/%x/%s/literal classes, which under WoW's C locale never match bytes >= 0x80. (4) Details does NOT byte-truncate long names — it clips them visually via lineText1:SetWidth + SetWordWrap(false) (Details/classes/class_damage.lua:3277-3287), so no half-UTF-8 character reaches ExtractName. (5) The 15-underscore destructure at util.lua:140 really does land on setID: Blizzard's doc lists it as return 16 (ItemDocumentation.lua:630) and Blizzard's own code uses select(16, C_Item.GetItemInfo(...)) at Blizzard_WeeklyRewards.lua:358. (6) iLvl 157 hits the final else in GetIlvlColor (util.lua:62) and renders grey — no threshold hole. (7) The suspicious-looking precedence at core.lua:1374 (`name and (realm and realm ~= "") and (...) or name`) is actually correct for all three cases including realm == "" (same-realm, no suffix). (8) Details stamps its item_level_pool with epoch time() on both writers (Details/core/inspect.lua:75 and :147), so our poolTime staleness arithmetic at core.lua:274-278 is comparing like with like — this one could have silently killed the whole Details fallback and does not.
- ui-settings: Installed vs reference divergence: there is none to report. E:/World of Warcraft/_retail_/Interface/AddOns/Details_iLvlDisplay is a symlink to /e/dev/gaming/wow-addons/Details_iLvlDisplay (verified with ls -la), so the running copy and the working tree are the same bytes. Every file:line above is therefore live.
- ui-settings: Whether InCombatLockdown() actually returns a secret value inside restricted instances. I could not run the game. The evidence is contradictory: the addon asserts it at secrets.lua:128-133 and ui/main_frame.lua:376-379, but the project's own secret radar records the opposite on live 12.0.7.68974 — e:/dev/gaming/_learnings/secret-watch/reports/2026-08-08.md:24 ("InCombatLockdown and C_InstanceEncounter.IsEncounterInProgress carry no Secret* fields") and baseline_api_secret_annotations.txt:65-67 ("# no Secret* fields; SecretReturns: NONE"), matching wow-ui-source RestrictedActionsDocumentation.lua:44-52. Finding 1's provable half (open window + real combat + /dilvl = closes with a false promise) holds either way; the "settings unreachable for the whole instance" amplification depends on which of the two is right, and this is exactly the Ambiguate class where docs and observed behaviour diverged.
- ui-settings: Whether ui/safe_callback.lua:126 and :134 (`body:SetWidth(parent:GetWidth() - 40)`) can go negative. main.frame.content is anchored on all four sides (main_frame.lua:283-286) so its width should resolve to ~696, but I could not confirm it is non-zero at the moment BuildPlaceholder runs on the very first SwitchTab. Note that BuildPlaceholder is the layer-2 RECOVERY path and is itself called unprotected at main_frame.lua:148 and :154 — if it throws, the content frame has already had all children unparented (safe_callback.lua:105-109) and the tab is left blank with the error escaping SwitchTab entirely.
- ui-settings: Blizzard's UIDropDownMenu internals. Finding 5's "blank dropdown" is inferred from ui/widgets.lua:225-236 calling UIDropDownMenu_SetText only inside the match loop and no other SetText call existing on the widget; I did not read UIDropDownMenuTemplate's source to confirm what the control renders when SetSelectedValue is given an unlisted value and SetText is never called.
- ui-settings: Parked-callback recovery from the UI. I found that ApplySettingChange("elvuiTag"/"grid2Status") calls only NotifyElvUI (core.lua:1771-1774) and never Details_iLvlDisplayAPI:RestoreCallback, which /dilvl elvui on and /dilvl grid2 on do call (core.lua:2458, 2471) — the comment at core.lua:2818-2819 names those two slash commands as the only un-park path. I did NOT report it as a finding because both callback bodies are internally pcall-wrapped (elvui_tags.lua:118-121, grid2_status.lua:71), so I could not construct a throw that would actually reach the counter at core.lua:2803 and park them. Worth a second look if either body ever gains an unprotected line. Related and unproven: Details_iLvlDisplay_DandersReset (danders_integration.lua:347-357) re-registers the DandersFrames-side callback but not API:RegisterCallback("danders", ...), so the "danders" and "blizzdm" registry entries have no restore path from either surface.
- ui-settings: ui/theme.lua and ui/preview_widgets.lua were read for their interaction with db reads and the fault-isolation wiring, but not audited for rendering correctness (anchor math, mock fidelity, ticker lifetime) — that is a different lens.
- ui-settings: The unwrapped tooltip handlers at ui/widgets.lua:102-108, 176-182, 242-248 and 284-290 use raw SetScript rather than safe.WrapScript, so an OnEnter throw bypasses all three layers and re-fires on every mouse-over. I could not construct a concrete throw for GameTooltip:SetOwner/SetText/AddLine with the values these call sites actually pass (all are L[...] lookups, and locales/enUS.lua:118 installs an identity-fallback metatable so none can be nil), so I am reporting it here rather than as a finding.